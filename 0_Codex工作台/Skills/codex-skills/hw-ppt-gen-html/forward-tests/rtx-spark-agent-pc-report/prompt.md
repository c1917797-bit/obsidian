# Prompt

请先读取并遵守仓库 `SKILL.md`，然后基于如下网页证据材料，制作一份中文 HTML 分析报告，目的是让读者理解这个技术，允许启动子agent。

```text
sources/web/
```

工作区要求:

```text
.tmp/html-ppt-runs/rtx-spark-agent-pc-report/<run-id>/
```

`<run-id>` 必须使用本次运行的唯一 ID，避免覆盖历史结果。不要改动 `sources/` 下的原始材料。
