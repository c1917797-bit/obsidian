# Prompt

请先读取并遵守仓库 `SKILL.md`，然后基于如下论文解析结果，制作一份HTML-PPT，信息密度要高，最多4页把事情讲清楚，允许启动子agent。

```text
sources/tidar/final/
```

工作区要求:

```text
.tmp/html-ppt-runs/tidar-paper-deck/<run-id>/
```

`<run-id>` 必须使用本次运行的唯一 ID，避免覆盖历史结果。不要改动 `sources/` 下的原始材料。
