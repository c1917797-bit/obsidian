# AI推理内容问题清单（可执行版）

更新时间：2026-09-06 13:00:00（基于本次复跑）

## 已验证状态（当前已执行）
- 已手工新增 1 条 `InferenceRadar` 候选卡：
  - `3_AI情报日历/Inbox/InferenceRadar/2026-09/2026-09-06_vLLM release 验证卡.md`
- 本地扫描结果：
  - `TOTAL=1`
  - `BAD=1`
  - `STALE=0`
  - `DUPLICATE=0`
- `BAD=1` 的原因是该卡 `evidence_score=0`（可视作“待核验”阶段条目）
- `sync-inference-github.ps1 status` 与 `sync-inference-arxiv.ps1 status` 均可正常运行
- `inference-audit.ps1 report` 已生成：
  - `3_AI情报日历/Inbox/InferenceRadar/问题清单.md`

## P0（阻断问题，当前最关键）
- 没有稳定输出的高质量“候选卡”流：`inference-radar` 里只有少量手工卡 + 自动报告文件。你当前还没形成“每周新增可复用候选流”。
- 现有卡片还未完成“可复核指标闭环”（TTFT/TPOT/Throughput/Peak memory/Quality delta）
- 原始来源字段未强制要求（例如 `primary_source` 为HTTPS）会让错误卡混入；当前审核脚本会拦截这一类问题。

## P1（质量与结构风险）
- 4_AI内仍有大量“占位/待检索/待补充”内容，若直接升到洞察会污染决策链：
  - `4_AI情报洞察/技术洞察/多卡协同/KVCache生成优化论文源列表.md`（当前占位密集）
  - `4_AI情报洞察/技术洞察/推理压缩/03_技术洞察/参数压缩技术洞察报告.md`（含“占位语义”）
  - `4_AI情报洞察/技术洞察/推理压缩/06_综合报告/量化稀疏蒸馏_产业学术课题对齐_v8.md`（内含“占位”注记）
  - `4_AI情报洞察/场景化创新/AI通话助手业界进展.md`（不是直接推理一手信号，建议转入产品分析，不建议当雷达源）
- 同时建议清理命名编码异常文件 `InferenceRadar` 下的乱码名文件（`é—®é¢˜æ¸…å•.md`）以便可维护。

## P2（流程优化，建议立即接入）
- 将 `new` 命令的默认模板字段从 `source_type: paper` 改为 `source_type: unknown` 或强制填充来源类型，减少误分类。
- 对人工卡统一要求：建立“最小可入库字段”检查：
  - `primary_source` 必须是 HTTPS 一手源
  - `evidence_score >= 2` 才算通过
  - `status=candidate` 时必须补齐版本/commit、指标或复核结论
- 将 `问题清单.md` 与清单页挂钩：每周执行一次并截图留档。

## 一周运行SOP（建议）
1. `cd C:\Users\Huawei\Documents\code\Obsidian\0_Codex工作台\tools`
2. `pwsh ./sync-inference-github.ps1 sync`
3. `pwsh ./sync-inference-arxiv.ps1 sync -MaxResults 20`
4. `pwsh ./inference-audit.ps1 scan`
5. `pwsh ./inference-audit.ps1 report`
6. 只保留 `BAD=0` 的候选；其余回填证据后再升级为 `4_AI` 洞察。

## 成果门槛（你目标的验收标准）
- 每周新候选 `inference-radar` >= 3 条，且证据链完整（来源-版本-指标-复核）
- 每周 `scan` 产出 BAD 下降到 ≤1（且为正在复核中的条目）
- 至少 1 条每周进入 `4_AI情报洞察` 的“已验证”结论
- 4_AI 内不含未标注来源类型、缺指标来源或明显占位字段的正式结论条目
