# KV Cache 压缩论文完整深度精读（全集）

> 精读时间：2026-06-28
> 总计：274篇
> 格式：问题背景 → 核心创新 → 关键数据 → 技术标签

---

## ★★ $\text{D}_{2}\text{O}$: Dynamic Discriminative Operations for Efficient Long-Context Inference of Large Language Models

**iclr2025** | 评分 5.8 | arXiv:2507.04416

### 问题背景

Efficient generative inference in Large Language Models (LLMs) is impeded by the growing memory demands of Key-Value (KV) cache, especially for longer sequences. Traditional KV Cache eviction strategies, which discard less critical KV-pairs based on attention scores, often degrade generation quality

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  A2ATS: Retrieval-Based KV Cache Reduction via Windowed Rotary Position Embedding and Query-Aware Vector Quantization

**acl2025** | arXiv:2502.12665

### 问题背景

Long context large language models (LLMs) pose significant challenges for efficient serving due to the large memory footprint and high access overhead of KV cache.Retrieval-based KV cache reduction methods can mitigate these challenges, typically by offloading the complete KV cache to CPU and retrie

### 核心创新

A2ATS: Windowed Rotary Position Embedding; query-aware vector quantization; heterogeneous inference architecture for KV offloading

### 关键数据

2.7x long context serving throughput improvement

### 技术标签

方法: 量化 | 对象: kv

---
##  Accelerating LLM Inference with Lossless Speculative Decoding Algorithms for Heterogeneous Vocabularies

**icml2025** | arXiv:2502.05202

### 问题背景

Accelerating the inference of large language models (LLMs) is a critical challenge in generative AI. Speculative decoding (SD) methods offer substantial efficiency gains by generating multiple tokens using a single target forward pass.

### 核心创新

Lossless SD with heterogeneous vocabularies; off-the-shelf drafter models; no retraining required

### 关键数据

Up to 2.8x speedup over autoregressive decoding

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Accurate KV Cache Eviction via Anchor Direction Projection for Efficient LLM Inference

**neurips2025** | 评分 4.2 | arXiv:2507.04416

### 问题背景

Key-Value (KV) cache eviction---which retains the KV pairs of the most important tokens while discarding less important ones---is a critical technique for optimizing both memory usage and inference latency in large language models (LLMs). However, existing approaches often rely on simple heuristics-

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  Accurate KV Cache Quantization with Outlier Tokens Tracing

**acl2025** | arXiv:2505.10938

### 问题背景

The impressive capabilities of Large Language Models (LLMs) come at the cost of substantial computational resources during deployment. While KV Cache can significantly reduce recomputation during inference, it also introduces additional memory overhead.

### 核心创新

Outlier tokens identification; exclude outliers from quantization; improves accuracy

### 关键数据

6.4x memory reduction; 2.3x throughput improvement

### 技术标签

方法: 量化 | 对象: kv, communication

---
## ★ Activated LoRA: Fine-tuned LLMs for Intrinsics

**neurips2025** | 评分 3.8 | arXiv:2504.12397

### 问题背景

Low-Rank Adaptation (LoRA) has emerged as a highly efficient framework for finetuning the weights of large foundation models, and has become the go-to method for data-driven customization of LLMs. Despite the promise of highly customized behaviors and capabilities, switching between relevant LoRAs i

### 核心创新

aLoRA: Adapter activates only after invocation; reuses base model KV cache; enables intrinsics

### 关键数据

Matches LoRA accuracy; instant activation; contributed to HuggingFace PEFT

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ Activations Aren't Cheap in LoRA, Weights Are

**iclr2025** | 评分 4.2

### 问题背景

LoRA has become the prevailing technique for finetuning large neural networks with limited computational resources. Historically, activations have been regarded as small and computationally inexpensive to manipulateâ€”a view reflected by LoRA, which leverages this assumption and adds a low-rank term

### 核心创新

LoRA has become the prevailing technique for finetuning large neural networks with limited computational resources

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight, activation

---
## ★ AcuRank: Uncertainty-Aware Adaptive Computation for Listwise Reranking

**neurips2025** | 评分 4.5 | arXiv:2505.18512

### 问题背景

Listwise reranking with large language models (LLMs) enhances top-ranked results in retrieval-based applications. Due to the limit in context size and high inference cost of long context, reranking is typically performed over a fixed size of small subsets, with the final ranking aggregated from thes

### 核心创新

AcuRank: Bayesian TrueSkill uncertainty estimation; adaptive computation adjustment

### 关键数据

Superior accuracy-efficiency trade-off (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  AdaDecode: Accelerating LLM Decoding with Adaptive Layer Parallelism

**icml2025** | arXiv:2506.03700

### 问题背景

Large language models (LLMs) are increasingly used for long-content generation (e.g., long Chain-of-Thought reasoning) where decoding efficiency becomes a critical bottleneck: Autoregressive decoding is inherently limited by its sequential token generation process, where each token must be generated

### 核心创新

AdaDecode: Adaptive layer parallelism; intermediate layer token generation; confidence-based skipping; deferred computation

### 关键数据

1.73x speedup with output parity

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ ADAPT: Adaptive Prompt Tuning for Pre-Trained Vision-Language Models

**iclr2025** | 评分 5.5

### 问题背景

Prompt tuning has emerged as an effective way for parameter-efficient fine-tuning. Conventional deep prompt tuning inserts continuous prompts of a fixed context length into the input to each layer.

### 核心创新

Prompt tuning has emerged as an effective way for parameter-efficient fine-tuning

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
##  Adaptive Mass-Segmented KV Compression for Long-Context Reasoning

**arXiv 2026** | arXiv:2605.23200

### 核心创新

Region-aware quota allocation prevents Region Wipe-out (contiguous reasoning block eviction); EMA-based smoothing; universal plug-and-play compatible with TOVA, KeyDiff, R-KV, TriAttention; vLLM compatible

### 关键数据

Mitigates structural fragmentation and boosts model performance across MATH500, AIME, GSM8K, code completion, QA, and retrieval

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ Adaptive Memory Mechanism in Vision Transformer for Long-form Video Understanding

**iclr2025** | 评分 4.6

### 问题背景

In long-form video understanding, selecting an optimal Temporal Receptive Field (TRF) is crucial for Vision Transformer (ViT) models due to the dynamic nature of diverse video motion contents, which varies in duration and velocity. A short TRF can result in loss of critical information, while a long

### 核心创新

In long-form video understanding, selecting an optimal Temporal Receptive Field (TRF) is crucial for Vision Transformer (ViT) models due to the dynamic nature of diverse video motion contents, which v...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
##  AdaSplash: Adaptive Sparse Flash Attention

**icml2025** | arXiv:2502.12082

### 问题背景

The computational cost of softmax-based attention in transformers limits their applicability to long-context tasks. Adaptive sparsity, of which $\alpha$-entmax attention is an example, offers a flexible data-dependent alternative, but existing implementations are inefficient and do not leverage the 

### 核心创新

AdaSplash: Hybrid Halley-bisection for alpha-entmax; Triton kernels for adaptive sparsity

### 关键数据

Approaches FlashAttention-2 efficiency

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ AdmTree: Compressing Lengthy Context with Adaptive Semantic Trees

**neurips2025** | 评分 4.2 | arXiv:2512.04550

### 问题背景

The quadratic complexity of self-attention limits Large Language Models (LLMs) in processing long contexts, a capability vital for many advanced applications. Context compression aims to mitigate this computational barrier while preserving essential semantic information.

### 核心创新

AdmTree: Adaptive semantic binary tree; gist tokens for variable-length segments; hierarchical abstraction

### 关键数据

Preserves semantic fidelity with efficiency

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  AMXFP4: Taming Activation Outliers with Asymmetric Microscaling Floating-Point for 4-bit LLM Inference

**acl2025** | arXiv:2411.09909

### 问题背景

As large language models (LLMs) grow in parameter size and context length, computation precision has been reduced from 16-bit to 4-bit to improve inference efficiency. However, this reduction causes accuracy degradation due to activation outliers.

### 核心创新

AMXFP4: Asymmetric FP format with shared scales; handles outliers and group-wise asymmetry; no calibration needed

### 关键数据

Outperforms MXFP4 by 3% on VQA; exceeds rotation-based by 1.6% on CSQA

### 技术标签

方法: 量化 | 对象: kv, weight, activation

---
##  AnchorKV: Safety-Aware KV Cache Compression via Soft Penalty with a Refusal Anchor

**arXiv 2026** | arXiv:2606.17872

### 核心创新

Soft penalty with refusal anchor in key projection space; difference-of-means representation engineering adapted to KV caching; safety-aware compression

### 关键数据

Substantially improved safety alignment vs original compressor with small utility tradeoff

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  AQPIM: Breaking the PIM Capacity Wall for LLMs with In-Memory Activation Quantization

**HPCA 2026** | arXiv:2604.18137

### 核心创新

PIM-specialized Product Quantization (PQ) for activations; quantization performed directly within memory; exploits PIM internal bandwidth

### 关键数据

Reduces GPU-CPU communication by 90-98.5% of decoding latency; 3.4x speedup over SOTA PIM approach

### 技术标签

方法: 量化 | 对象: activation

---
## ★★ ASVD: Activation-aware Singular Value Decomposition for Compressing Large Language Models

**iclr2025** | 评分 6.2 | arXiv:2312.05821

### 问题背景

In this paper, we introduce a new post-training compression paradigm for Large Language Models (LLMs) to facilitate their wider adoption. We delve into LLM weight low-rank decomposition, and find that the challenges of this task stem from â¶ the distribution variance in the LLM activations and â· 

### 核心创新

ASVD: Activation-aware Singular Value Decomposition; low-rank decomposition for KV cache compression

### 关键数据

Efficient LLM compression via activation-aware SVD

### 技术标签

方法: 低秩 | 对象: kv, weight, activation

---
##  Attention-Level Speculation

**icml2025**

### 问题背景

As Large Language Models (LLMs) grow in size and context length, efficient inference strategies are essential to maintain low-latency token generation. Unfortunately, conventional tensor and data parallelism face diminishing returns when scaling across multiple devices.

### 核心创新

As Large Language Models (LLMs) grow in size and context length, efficient inference strategies are essential to maintain low-latency token generation

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, communication

---
##  AudioKV: KV Cache Eviction in Efficient Large Audio Language Models

**arXiv 2026** | arXiv:2604.06694

### 核心创新

Semantic-acoustic alignment identifies audio-critical attention heads; Spectral Score Smoothing (FFT-based global filtering); modality-specialized compression for audio

### 关键数据

At 40% compression ratio, maintains near-full accuracy on Qwen3-Omni-30B with only 0.45% drop vs catastrophic degradation in baselines

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ Autoregressive Adversarial Post-Training for Real-Time Interactive Video Generation

**neurips2025** | 评分 4.6 | arXiv:2507.04416

### 问题背景

Existing large-scale video generation models are computationally intensive, preventing adoption in real-time and interactive applications. In this work, we propose autoregressive adversarial post-training (AAPT) to turn a pre-trained latent video diffusion model into
a real-time, interactive, stream

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★★ Beyond Autoregression: Fast LLMs via Self-Distillation Through Time

**iclr2025** | 评分 7.0

### 问题背景

Autoregressive (AR) Large Language Models (LLMs) have demonstrated significant success across numerous tasks. However, the AR modeling paradigm presents certain limitations; for instance, contemporary autoregressive LLMs are trained to generate one token at a time, which can result in noticeable lat

### 核心创新

Diffusion language models generating 32 tokens simultaneously via self-distillation; 32-64x fewer inference steps

### 关键数据

8x faster than AR models with KV-caching at 1.3B scale

### 技术标签

方法: 蒸馏 | 对象: kv, weight

---
##  Beyond Decoder-only: Large Language Models Can be Good Encoders for Machine Translation

**acl2025** | arXiv:2503.06594

### 问题背景

The field of neural machine translation (NMT) has changed with the advent of large language models (LLMs). Much of the recent emphasis in natural language processing (NLP) has been on modeling machine translation and many other problems using a single pre-trained Transformer decoder, while encoder-d

### 核心创新

LLMs as NMT encoders; 75% KV cache reduction; cross-task generalization

### 关键数据

2.4-6.5x inference speedup (ACL Findings 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Boosting Long-Context Information Seeking via Query-Guided Activation Refilling

**acl2025**

### 问题背景

Processing long contexts poses a significant challenge for large language models (LLMs) due to their inherent context window limitations and the computational burden of extensive key-value (KV) activations, which severely impact efficiency. For information-seeking tasks, full context perception is o

### 核心创新

Processing long contexts poses a significant challenge for large language models (LLMs) due to their inherent context window limitations and the computational burden of extensive key-value (KV) activa...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, activation

---
## ★ Boosting Long-Context LLM Inference Efficiency with Intra-Layer Attention Similarity

**iclr2025** | 评分 4.8

### 问题背景

The increasing context window size in Large Language Models (LLMs), such as the GPT and LLaMA series, has improved their ability to tackle complex, long-text tasks, but at the cost of inference efficiency, particularly regarding memory and computational complexity. Existing methods, including select

### 核心创新

The increasing context window size in Large Language Models (LLMs), such as the GPT and LLaMA series, has improved their ability to tackle complex, long-text tasks, but at the cost of inference effici...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Cache Me If You Must: Adaptive Key-Value Quantization for Large Language Models

**icml2025**

### 问题背景

Efficient real-world deployments of large language models (LLMs) rely on Key-Value (KV) caching for processing and generating long outputs, reducing the need for repetitive computation. For large contexts, Key-Value caches can take up tens of gigabytes of device memory, as they store vector represen

### 核心创新

Efficient real-world deployments of large language models (LLMs) rely on Key-Value (KV) caching for processing and generating long outputs, reducing the need for repetitive computation

### 关键数据

See abstract for details

### 技术标签

方法: 量化, 驱逐/剪枝, 低秩 | 对象: kv, weight

---
##  Can Compressed LLMs Truly Act? An Empirical Evaluation of Agentic Capabilities in LLM Compression

**icml2025**

### 问题背景

Post-training compression reduces the computational and memory costs of large language models (LLMs), enabling resource-efficient deployment. However, existing compression benchmarks focus narrowly on language modeling (e.g., perplexity) and natural language understanding tasks (e.g., GLUE accuracy)

### 核心创新

Post-training compression reduces the computational and memory costs of large language models (LLMs), enabling resource-efficient deployment

### 关键数据

See abstract for details

### 技术标签

方法: 量化, 驱逐/剪枝, 稀疏化 | 对象: kv, weight

---
##  CAOTE: Efficient Caching through Attention Output based Token Eviction

**neurips2025** | 评分 2.5 | arXiv:2504.14051

### 问题背景

While long context support of large language models has extended their abilities, it also incurs challenges in memory and compute which becomes crucial bottlenecks in resource-restricted devices. Token eviction, a widely adopted post-training methodology designed to alleviate the bottlenecks by evic

### 核心创新

CAOTE: Attention output error-based token eviction; optimizes eviction error; integrates attention scores and value vectors

### 关键数据

Efficient KV cache selection for LLMs

### 技术标签

方法: 通用优化 | 对象: kv

---
##  CASAK-V: Dynamic Sparse Attention and Adaptive KV-Cache Compression for Memory-Efficient Long-Context LLM Inference

**iclr2025**

### 问题背景

The emergence of long-context Large Language Models (LLMs) has triggered a rapid expansion of applications across various domains. However, these models remain inaccessible for on-device or on-premises deployments due to significant computational and memory challenges.

### 核心创新

Scalable multi-cartridge learning with dynamic distractor mixing; memory-efficient budget manager; hundreds of per-document cartridges

### 关键数据

10-31 points improvement over monolithic cartridge; 3-4x fewer prompt tokens vs RAG

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ CASD: Enhancing Generation Accuracy via Context-Aware Speculative Decoding

**iclr2025** | 评分 3.0

### 问题背景

With recent advancements in long-context model variants, Large language models (LLMs) can conveniently process different types of task-related information by simply converting them into an input sequence, even consisting of over 100K tokens. Though with a simple and unified form, there is still cons

### 核心创新

Scalable multi-cartridge learning with dynamic distractor mixing; memory-efficient budget manager; hundreds of per-document cartridges

### 关键数据

10-31 points improvement over monolithic cartridge; 3-4x fewer prompt tokens vs RAG

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Cassandra: Enabling Reasoning LLMs at Edge via Self-Speculative Decoding

**ISCA 2026** | arXiv:2605.26558

### 核心创新

Scalable multi-cartridge learning with dynamic distractor mixing; memory-efficient budget manager; hundreds of per-document cartridges

### 关键数据

10-31 points improvement over monolithic cartridge; 3-4x fewer prompt tokens vs RAG

### 技术标签

方法: 驱逐/剪枝, 量化 | 对象: kv, weight

---
##  CateKV: On Sequential Consistency for Long-Context LLM Inference Acceleration

**icml2025** | arXiv:2507.04416

### 问题背景

Large language models (LLMs) have demonstrated strong capabilities in handling long-context tasks, but processing such long contexts remains challenging due to the substantial memory requirements and inference latency. In this work, we discover that certain attention heads exhibit sequential consist

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ ChunkKV: Semantic-Preserving KV Cache Compression for Efficient Long-Context LLM Inference

**neurips2025** | 评分 4.0

### 问题背景

Large Language Models (LLMs) require significant GPU memory when processing long texts, with the key value (KV) cache consuming up to 70\% of total memory during inference. Although existing compression methods reduce memory by evaluating the importance of individual tokens, they overlook critical s

### 核心创新

Semantic chunk as basic compression unit (not isolated tokens); preserves linguistic structures and contextual integrity; layer-wise index reuse

### 关键数据

8.7% precision improvement; 26.5% throughput improvement at same compression ratio

### 技术标签

方法: 通用优化 | 对象: kv

---
##  ClusterAttn: KV Cache Compression under Intrinsic Attention Clustering

**acl2025** | arXiv:2507.04416

### 问题背景

Sparse attention can effectively alleviate the significant demands on memory when large language models (LLMs) process long contexts. Existing methods typically apply the same sparse pattern across different attention heads and inputs.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  CodeComp: Structural KV Cache Compression for Agentic Coding

**arXiv 2026** | arXiv:2604.10235

### 核心创新

Code Property Graph priors from Joern static analysis; incorporates structural program analysis into KV compression; training-free; SGLang compatible

### 关键数据

Recovers majority of full-context accuracy under aggressive compression; matches patch generation quality of uncompressed inference

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  CommVQ: Commutative Vector Quantization for KV Cache Compression

**icml2025** | arXiv:2506.18879

### 问题背景

Large Language Models (LLMs) are increasingly used in applications requiring long context lengths, but the key-value (KV) cache often becomes a memory bottleneck on GPUs as context grows. To address this, we propose Commutative Vector Quantization (CommVQ) to significantly reduce memory usage for lo

### 核心创新

CommVQ: Commutative vector quantization; additive quantization with lightweight encoder and codebook

### 关键数据

Significant memory reduction for long-context LLM inference

### 技术标签

方法: 量化 | 对象: kv, weight

---
## ★ Compress, Gather, and Recompute: REFORMing Long-Context Processing in Transformers

**neurips2025** | 评分 3.8 | arXiv:2506.01215

### 问题背景

As large language models increasingly gain popularity in real-world applications, processing extremely long contexts, often exceeding the modelâ€™s pre-trained context limits, has emerged as a critical challenge. While existing approaches to efficient long-context processing show promise, recurrent 

### 核心创新

REFORM: Two-phase long-context processing; compress, gather, recompute

### 关键数据

Efficient long-context handling with information preservation

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Compute or Load KV Cache? Why Not Both?

**icml2025**

### 问题背景

Large Language Models (LLMs) are increasingly deployed in large-scale online services, enabling sophisticated applications. However, the computational overhead of generating key-value (KV) caches in the prefill stage presents a major bottleneck, particularly for long-context inputs.

### 核心创新

Large Language Models (LLMs) are increasingly deployed in large-scale online services, enabling sophisticated applications

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ COrAL: Order-Agnostic Language Modeling for Efficient Iterative Refinement

**iclr2025** | 评分 5.8 | arXiv:2507.04416

### 问题背景

Iterative refinement has emerged as an effective paradigm for enhancing the capabilities of large language models (LLMs) on complex tasks. However, existing approaches typically implement iterative refinement at the application or prompting level, relying on autoregressive (AR) modeling.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Core Context Aware Attention for Long Context Language Modeling

**iclr2025** | 评分 4.7 | arXiv:2412.12465

### 问题背景

Transformer-based Large Language Models (LLMs) have exhibited remarkable success in various natural language processing tasks primarily attributed to self-attention mechanism, which requires a token to consider all preceding tokens as its context to compute the attention score. However, when the con

### 核心创新

CCA-Attention: Globality-aware pooling + locality-preserving module; plug-and-play self-attention replacement

### 关键数据

Superior long-context modeling (ICML 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Core Context Aware Transformers for Long Context Language Modeling

**icml2025** | arXiv:2412.12465

### 问题背景

Transformer-based Large Language Models (LLMs) have exhibited remarkable success in extensive tasks primarily attributed to self-attention mechanism, which requires a token to consider all preceding tokens as its context to compute attention. However, when the context length L becomes very large (e.

### 核心创新

CCA-Attention: Globality-aware pooling + locality-preserving module; plug-and-play self-attention replacement

### 关键数据

Superior long-context modeling (ICML 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Cost-efficient Collaboration between On-device and Cloud Language Models

**icml2025** | arXiv:2507.04416

### 问题背景

We investigate an emerging setup in which a small, on-device language model (LM) with access to local data collaborates with a frontier, cloud-hosted LM to solve real-world tasks involving financial, medical, and scientific reasoning over long documents. *Can a local-remote collaboration reduce clou

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Cramming 1568 Tokens into a Single Vector and Back Again: Exploring the Limits of Embedding Space Capacity

**acl2025** | arXiv:2502.13063

### 问题背景

A range of recent works addresses the problem of compression of sequence of tokens into a shorter sequence of real-valued vectors to be used as inputs instead of token embeddings or key-value cache. These approaches are focused on reduction of the amount of compute in existing language models rather

### 核心创新

Per-sample optimization for extreme compression; up to 1500x compression ratio

### 关键数据

Two orders of magnitude beyond existing methods (ACL 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Curse of High Dimensionality Issue in Transformer for Long Context Modeling

**icml2025** | arXiv:2505.22107

### 问题背景

Transformer-based large language models (LLMs) excel in natural language processing tasks by capturing long-range dependencies through self-attention mechanisms. However, long-context modeling faces significant computational inefficiencies due to redundant attention computations: while attention wei

### 核心创新

DGA: Dynamic Group Attention; group coding strategy; supervised learning reformulation

### 关键数据

Significant compute reduction (ICML 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, weight, activation

---
## ★ Customize Your Visual Autoregressive Recipe with Set Autoregressive Modeling

**iclr2025** | 评分 4.6

### 问题背景

We introduce a new paradigm for AutoRegressive (AR) image generation, termed Set AutoRegressive Modeling (SAR). SAR generalizes the conventional AR to the next-set setting, i.e., splitting the sequence into arbitrary sets containing multiple tokens, rather than outputting each token in a fixed raste

### 核心创新

We introduce a new paradigm for AutoRegressive (AR) image generation, termed Set AutoRegressive Modeling (SAR)

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight

---
##  DAM: Dynamic Attention Mask for Long-Context Large Language Model Inference Acceleration

**acl2025** | arXiv:2506.11104

### 问题背景

Long-context understanding is crucial for many NLP applications, yet transformers struggle with efficiency due to the quadratic complexity of self-attention. Sparse attention methods alleviate this cost but often impose static, predefined masks, failing to capture heterogeneous attention patterns.

### 核心创新

DAM: Dynamic sparse attention with adaptive masks; context-aware attention structures

### 关键数据

Reduces memory and compute overhead; minimal performance degradation

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★★ DeciMamba: Exploring the Length Extrapolation Potential of Mamba

**iclr2025** | 评分 5.0 | arXiv:2312.00752

### 问题背景

Long-range sequence processing poses a significant challenge for Transformers due to their quadratic complexity in input length. A promising alternative is Mamba, which demonstrates high performance and achieves Transformer-level capabilities while requiring substantially fewer computational resourc

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: unclear | 对象: kv

---
## ★ Decoder-Hybrid-Decoder Architecture for Efficient Reasoning with Long Generation

**neurips2025** | 评分 4.2 | arXiv:2507.04416

### 问题背景

Recent advances in language modeling have demonstrated the effectiveness of State Space Models (SSMs) for efficient sequence modeling. While hybrid architectures such as Samba and the decoder-decoder architecture, YOCO, have shown promising performance gains over Transformers, prior works have not i

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Demystifying Small Language Models for Edge Deployment

**acl2025**

### 问题背景

Small language models (SLMs) have emerged as a promising solution for deploying resource-constrained devices, such as smartphones and Web of Things. This work presents the first comprehensive study of over 60 SLMs such as Microsoft Phi and Google Gemma that are publicly accessible.

### 核心创新

Small language models (SLMs) have emerged as a promising solution for deploying resource-constrained devices, such as smartphones and Web of Things

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ DenseAttention: No-Compromise Exact All $N \times N$ Interactions Algorithm with $O(N)$ Space and Time Complexity

**iclr2025** | 评分 4.5

### 问题背景

The ubiquitous Transformer architecture suffers from two main bottlenecks: 1) low computational and memory efficiency, leading to suboptimal hardware utilization, and 2) quadratic time complexity with respect to sequence length $N$, making it slow and costly for large data contexts. We propose a nov

### 核心创新

The ubiquitous Transformer architecture suffers from two main bottlenecks: 1) low computational and memory efficiency, leading to suboptimal hardware utilization, and 2) quadratic time complexity with...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, activation

---
##  Design Choices for Extending the Context Length of Visual Language Models

**acl2025**

### 问题背景

Visual Language Models (VLMs) demonstrate impressive capabilities in processing multimodal inputs, yet applications such as visual agents, which require handling multiple images and high-resolution videos, demand enhanced long-range modeling. Moreover, existing open-source VLMs lack systematic explo

### 核心创新

Visual Language Models (VLMs) demonstrate impressive capabilities in processing multimodal inputs, yet applications such as visual agents, which require handling multiple images and high-resolution vi...

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★★ Disentangling Latent Shifts of In-Context Learning Through Self-Training

**iclr2025** | 评分 5.8 | arXiv:2410.01508

### 问题背景

In-context learning (ICL) has become essential in natural language processing, particularly with autoregressive large language models capable of learning from demonstrations provided within the prompt. However, ICL faces challenges with stability and long contexts, especially as the number of demons

### 核心创新

Parameter-efficient ICL disentanglement; teacher-student weak supervision; pseudo-label correction

### 关键数据

Outperforms standard ICL and prior methods (NeurIPS 2025)

### 技术标签

方法: 蒸馏, 低秩 | 对象: kv

---
## ★ dKV-Cache: The Cache for Diffusion Language Models

**neurips2025** | 评分 4.0 | arXiv:2505.15781

### 问题背景

Diffusion Language Models (DLMs) have been seen as a promising competitor for autoregressive language models (ARs). However, diffusion language models have long been constrained by slow inference.

### 核心创新

dKV-Cache: Delayed KV cache for diffusion language models; two variants (Decode/Greedy)

### 关键数据

2-10x inference speedup for DLMs; training-free

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ Donâ€™t Discard, but Keep It Small: Context-Preserving KV Cache Compression with Importance-Aware Adaptive Precision

**iclr2025** | 评分 5.0

### 问题背景

As the length of input sequences in Large Language Models (LLMs) continues to grow, efficient key-value (KV) cache management has become essential for improving inference speed and throughput of autoregressive decoding. Although several approaches have been proposed to reduce memory usage by selecti

### 核心创新

As the length of input sequences in Large Language Models (LLMs) continues to grow, efficient key-value (KV) cache management has become essential for improving inference speed and throughput of autor...

### 关键数据

See abstract for details

### 技术标签

方法: 量化 | 对象: kv

---
##  Don't Waste Bits! Adaptive KV-Cache Quantization for Lightweight On-Device LLMs

**CVPR 2026** | arXiv:2604.04722

### 核心创新

Learned adaptive quantization policy assigning bit-width proportional to token importance; features include token frequency, quality score, attention variance, entropy-based uncertainty; dynamic precision selection {2-bit, 4-bit, 8-bit, FP16}

### 关键数据

17.75% latency reduction on HellaSwag; 7.60 accuracy points improvement; within 0.30 points of FP16

### 技术标签

方法: 量化 | 对象: kv

---
##  DRAMA: Diverse Augmentation from Large Language Models to Smaller Dense Retrievers

**acl2025** | arXiv:2502.18460

### 问题背景

Large language models (LLMs) have demonstrated strong effectiveness and robustness when fine-tuned as dense retrievers.However, their large parameter size presents significant computational challenges at inference time.While smaller retrievers offer better efficiency, they often fail to generalize e

### 核心创新

DRAMA: Pruned LLM backbone + diverse LLM-augmented data; single-stage contrastive learning

### 关键数据

Better multilingual and long-context than traditional retrievers (ACL 2025)

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★ DuSA: Fast and Accurate Dual-Stage Sparse Attention Mechanism Accelerating Both Training and Inference

**neurips2025** | 评分 3.8 | arXiv:2507.04416

### 问题背景

This paper proposes the Dual-Stage Sparse Attention (DuSA) mechanism for attention acceleration of transformers. In the first stage, DuSA performs intrablock sparse attention to aggregate local inductive biases.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  Dustin: Draft-Augmented Sparse Verification for Efficient Long-Context Generation with Speculative Decoding

**ICML 2026** | arXiv:2606.24957

### 核心创新

Sparse verification combining lookahead signals from draft with historical attention; sparse estimation restricting importance scoring to minimal subset of attention heads

### 关键数据

27.85x speedup in self-attention; 9.17x end-to-end decoding speedup at 32k sequence length; negligible accuracy degradation

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  DyCoke: Dynamic Compression of Tokens for Fast Video Large Language Models

**cvpr2025** | arXiv:2411.15024

### 问题背景

Video large language models (VLLMs) have significantly advanced recently in processing complex video content. Yet, their inference efficiency remains constrained because of the high computational cost stemming from the thousands of visual tokens generated from the video inputs.

### 核心创新

DyCoke: Temporal compression module; dynamic KV cache reduction across frames; training-free

### 关键数据

1.5x inference speedup; 1.4x memory reduction

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★★ Dynamic-LLaVA: Efficient Multimodal Large Language Models via Dynamic Vision-language Context Sparsification

**iclr2025** | 评分 6.0 | arXiv:2412.00876

### 问题背景

Multimodal Large Language Models (MLLMs) have achieved remarkable success in vision understanding, reasoning, and interaction. However, the inference computation and memory increase progressively with the generation of output tokens during decoding, directly affecting the efficacy of MLLMs.

### 核心创新

Dynamic-LLaVA: Dynamic vision-language context sparsification; tailored schemes per inference mode

### 关键数据

~75% prefill computation reduction (ICLR 2025)

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★★ E2LLM: Encoder Elongated Large Language Models for Long-Context Understanding and Reasoning

**iclr2025** | 评分 5.6 | arXiv:2409.06679

### 问题背景

In the realm of Large Language Models (LLMs), the ability to process long contexts is increasingly crucial for tasks such as multi-round dialogues, code generation, and document summarization. This paper addresses the challenges of achieving high long-context performance, low computational complexit

### 核心创新

E2LLM: Chunked context + text encoder compression + decoder alignment via adapter; reconstruction + instruction fine-tuning

### 关键数据

Outperforms 8 SOTA methods (EMNLP 2025)

### 技术标签

方法: 低秩 | 对象: kv

---
## ★ EasySpec: Layer-Parallel Speculative Decoding for Efficient Multi-GPU Utilization

**neurips2025** | 评分 4.0 | arXiv:2502.02493

### 问题背景

Speculative decoding is an effective and lossless method for Large Language Model (LLM) inference acceleration. It employs a smaller model to generate a draft token sequence, which is then verified by the original base model.

### 核心创新

EasySpec: Layer-parallel speculation; breaks inter-layer dependencies; KV cache calibration

### 关键数据

4.17x peak speedup; 1.62x draft acceleration; training-free

### 技术标签

方法: 通用优化 | 对象: kv, communication

---
##  EchoKV: Efficient KV Cache Compression via Similarity-Based Reconstruction

**arXiv 2026** | arXiv:2603.22910

### 核心创新

Reconstructs discarded KV from partial subset using lightweight network; exploits inter-layer/intra-layer attention head similarities; on-demand full/compressed cache switching; lightweight 2-stage fine-tuning (few minutes on single A100 for 7B)

### 关键数据

Outperforms existing methods across compression ratios on LongBench and RULER; preserves full-cache throughput in short-context scenarios

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  ECoRAG: Evidentiality-guided Compression for Long Context RAG

**acl2025** | arXiv:2506.05167

### 问题背景

Large Language Models (LLMs) have shown remarkable performance in Open-Domain Question Answering (ODQA) by leveraging external documents through Retrieval-Augmented Generation (RAG). To reduce RAG overhead, from longer context, context compression is necessary.

### 核心创新

ECoRAG: Evidentiality-guided compression; reflective re-retrieval for insufficient evidence

### 关键数据

Improves ODQA performance; cost-efficient

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Efficient and Privacy-Preserving Soft Prompt Transfer for LLMs

**icml2025**

### 问题背景

Prompting has become a dominant paradigm for adapting large language models (LLMs). While discrete (textual) prompts are widely used for their interpretability, soft (parameter) prompts have recently gained traction in APIs.

### 核心创新

Prompting has become a dominant paradigm for adapting large language models (LLMs)

### 关键数据

See abstract for details

### 技术标签

方法: 蒸馏 | 对象: kv, weight

---
##  Efficient Long Context Language Model Retrieval with Compression

**acl2025**

### 问题背景

Long Context Language Models (LCLMs) have emerged as a new paradigm to perform Information Retrieval (IR), which enables the direct ingestion and retrieval of information by processing an entire corpus in their single context, showcasing the potential to surpass traditional sparse and dense retrieva

### 核心创新

Long Context Language Models (LCLMs) have emerged as a new paradigm to perform Information Retrieval (IR), which enables the direct ingestion and retrieval of information by processing an entire corpu...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★ Efficient Low Rank Attention for Long-Context Inference in Large Language Models

**neurips2025** | 评分 4.5

### 问题背景

As the length of input text grows, the key-value (KV) cache in LLMs imposes prohibitive GPU memory costs and limits longâ€context inference on resourceâ€constrained devices. Existing approaches, such as KV quantization and pruning, reduce memory usage but suffer from numerical precision loss or su

### 核心创新

As the length of input text grows, the key-value (KV) cache in LLMs imposes prohibitive GPU memory costs and limits longâ€context inference on resourceâ€constrained devices

### 关键数据

See abstract for details

### 技术标签

方法: 量化, 驱逐/剪枝, 稀疏化 | 对象: kv, weight

---
##  Efficient Multi-modal Long Context Learning for Training-free Adaptation

**icml2025**

### 问题背景

Traditional approaches to adapting multi-modal large language models (MLLMs) to new tasks have relied heavily on fine-tuning. This paper introduces Efficient Multi-Modal Long Context Learning (EMLoC), a novel training-free alternative that embeds demonstration examples directly into the model input.

### 核心创新

Traditional approaches to adapting multi-modal large language models (MLLMs) to new tasks have relied heavily on fine-tuning

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★ Efficient Prompt Compression with Evaluator Heads for Long-Context Transformer Inference

**neurips2025** | 评分 4.8 | arXiv:2501.12959

### 问题背景

Although applications involving long-context inputs are crucial for the effective utilization of large language models (LLMs), they also result in increased computational costs and reduced performance. To address this challenge, we propose an efficient, training-free prompt compression method that r

### 核心创新

EHPC: Evaluator head-based prompt compression; skim first layers; training-free

### 关键数据

SOTA prompt compression; competitive with KV cache acceleration

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Efficiently Serving Large Multimodal Models Using EPD Disaggregation

**icml2025** | arXiv:2501.05460

### 问题背景

Large Multimodal Models (LMMs) extend Large Language Models (LLMs) by handling diverse inputs such as images, audio, and video, but at the cost of adding a multimodal encoding stage that increases both computational and memory overhead. This step negatively affects key Service Level Objectives (SLOs

### 核心创新

EPD: Encodes-Prefill-Decode separation; multimedia token caching; role-switching

### 关键数据

15x lower peak memory; 22x larger batch; 71% TTFT reduction (ICML 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ EfficientNav: Towards On-Device Object-Goal Navigation with Navigation Map Caching and Retrieval

**neurips2025** | 评分 4.2 | arXiv:2510.18546

### 问题背景

Object-goal navigation (ObjNav) tasks an agent with navigating to the location of a specific object in an unseen environment. Embodied agents equipped with large language models (LLMs) and online constructed navigation maps can perform ObjNav in a zero-shot manner.

### 核心创新

EfficientNav: Semantics-aware memory retrieval; discrete memory caching; attention-based clustering

### 关键数据

11.1% success rate improvement; 6.7x real-time (NeurIPS 2025)

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ ElasticMM: Efficient Multimodal LLMs Serving with Elastic Multimodal Parallelism

**neurips2025** | 评分 4.5 | arXiv:2507.10069

### 问题背景

Multimodal large language models (MLLMs) extend LLMs to handle images, videos, and audio by incorporating feature extractors and projection modules. However, these additional componentsâ€”combined with complex inference pipelines and heterogeneous workloadsâ€”introduce significant inference overhead

### 核心创新

ElasticMM: Elastic Multimodal Parallelism (EMP); adaptive parallelism per inference stage

### 关键数据

4.2x TTFT reduction; 3.2-4.5x throughput (NeurIPS 2025 Oral)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Embracing Collaboration Over Competition: Condensing Multiple Prompts for Visual In-Context Learning

**cvpr2025** | arXiv:2507.04416

### 问题背景

Visual In-Context Learning (VICL) enables adaptively solving vision tasks by leveraging pixel demonstrations, mimicking human-like task completion through analogy. Prompt selection is critical in VICL, but current methods assume the existence of a single "ideal" prompt in a pool of candidates, which

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 蒸馏 | 对象: kv, weight

---
## ★★★ Enhancing Zeroth-order Fine-tuning for Language Models with Low-rank Structures

**iclr2025** | 评分 7.0

### 问题背景

Parameter-efficient fine-tuning (PEFT) significantly reduces memory costs when adapting large language models (LLMs) for downstream applications. However, traditional first-order (FO) fine-tuning algorithms incur substantial memory overhead due to the need to store activation values for back-propaga

### 核心创新

Parameter-efficient fine-tuning (PEFT) significantly reduces memory costs when adapting large language models (LLMs) for downstream applications

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight, activation

---
##  EPIC: Efficient Position-Independent Caching for Serving Large Language Models

**icml2025** | arXiv:2410.15332

### 问题背景

Large Language Models (LLMs) show great capabilities in a wide range of applications, but serving them efficiently becomes increasingly challenging as requests (prompts) become more complex. Context caching improves serving performance by reusing Key-Value (KV) vectors, the intermediate representati

### 核心创新

EPIC: Position-Independent Caching (PIC); LegoLink algorithm for attention sink mitigation

### 关键数据

8x TTFT improvement; 7x throughput gain

### 技术标签

方法: 通用优化 | 对象: kv

---
##  EpMAN: Episodic Memory AttentioN for Generalizing to Longer Contexts

**acl2025** | arXiv:2502.14280

### 问题背景

Recent advances in Large Language Models (LLMs) have yielded impressive successes on many language tasks. However, efficient processing of long contexts using LLMs remains a significant challenge.

### 核心创新

EpMAN: Episodic memory module + episodic attention reweighing; semantically relevant chunk selection

### 关键数据

Robust performance across 16k-256k tokens

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Evolving Prompts In-Context: An Open-ended, Self-replicating Perspective

**icml2025**

### 问题背景

We propose a novel prompt design paradigm that challenges conventional wisdom in large language model (LLM) prompting. While conventional wisdom prioritizes well-crafted instructions and demonstrations for in-context learning (ICL), we show that pruning random demonstrations into seemingly incoheren

### 核心创新

We propose a novel prompt design paradigm that challenges conventional wisdom in large language model (LLM) prompting

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
##  EXIT: Context-Aware Extractive Compression for Enhancing Retrieval-Augmented Generation

**acl2025** | arXiv:2507.04416

### 问题背景

We introduce EXIT, an extractive context compression framework that enhances both the effectiveness and efficiency of retrieval-augmented generation (RAG) in question answering (QA). Current RAG systems often struggle when retrieval models fail to rank the most relevant documents, leading to the inc

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Fast and Accurate Language Model Decoding via Parallel Token Processing

**iclr2025** | 评分 4.0 | arXiv:2507.04416

### 问题背景

Autoregressive decoding suffers from an inherent efficiency bottleneck due to its sequential token generation process, where each token must be generated before the next can be processed. This sequential dependency significantly limits the ability to fully exploit the parallel processing power of mo

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ Fine-grained Attention I/O Complexity: Comprehensive Analysis for Backward Passes

**iclr2025** | 评分 6.3 | arXiv:2410.09397

### 问题背景

Large Language Models (LLMs) have demonstrated remarkable capabilities in processing long-context information. However, the quadratic complexity of attention computation with respect to sequence length poses significant computational challenges, and I/O aware algorithms have been proposed.

### 核心创新

Red-blue pebble game framework; tight I/O bounds for forward + backward; novel small-cache algorithm

### 关键数据

Validates FlashAttention optimality; theoretical tight bounds

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★ FLAME: Fast Long-context Adaptive Memory for Event-based Vision

**neurips2025** | 评分 4.0

### 问题背景

We propose Fast Long-range Adaptive Memory for Event (FLAME), a novel scalable architecture that combines neuro-inspired feature extraction with robust structured sequence modeling
to efficiently process asynchronous and sparse event camera data. As a departure from conventional input encoding metho

### 核心创新

We propose Fast Long-range Adaptive Memory for Event (FLAME), a novel scalable architecture that combines neuro-inspired feature extraction with robust structured sequence modeling
to efficiently proc...

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝, 稀疏化, 低秩 | 对象: kv, weight

---
## ★★★ FlashMask: Efficient and Rich Mask Extension of FlashAttention

**iclr2025** | 评分 7.0

### 问题背景

The computational and memory demands of vanilla attention scale quadratically with the sequence length $N$, posing significant challenges for processing long sequences in Transformer models. FlashAttention alleviates these challenges by eliminating the $\mathcal{O}(N^2)$ memory dependency and reduci

### 核心创新

IO-aware exact attention algorithm; tiling to reduce HBM SRAM reads/writes; block-sparse attention extension

### 关键数据

15% end-to-end speedup on BERT-large; 3x speedup on GPT-2; enables Path-X challenge (16K seq)

### 技术标签

方法: 稀疏化, 低秩 | 对象: kv, weight, activation

---
##  FlashMemory-DeepSeek-V4: Lightning Index Ultra-Long Context via Lookahead Sparse Attention

**arXiv 2026** | arXiv:2606.09079

### 核心创新

Lookahead Sparse Attention (LSA) with Neural Memory Indexer; proactively predicts future context demands; preserves only query-critical KV chunks

### 关键数据

Ultra-long context serving with reduced GPU memory bottleneck

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★★ FreqKV: Frequency Domain Key-Value Compression for Efficient Context Window Extension

**iclr2025** | 评分 5.8 | arXiv:2505.00570

### 问题背景

Extending the context window in large language models (LLMs) is essential for applications involving long-form content generation. However, the quadratic complexity of self-attention and the linear increase in key-value (KV) cache memory requirements with respect to sequence length present significa

### 核心创新

FreqKV: Frequency domain KV compression; low-frequency components; parameter-free

### 关键数据

256K context extension for LLaMA-2-7B; robust context window extension

### 技术标签

方法: 稀疏化, 低秩 | 对象: kv, weight

---
##  From Rigid to Dynamic: Entropy-Guided Adaptive Inference for Long-Context LLMs

**arXiv 2026** | arXiv:2606.09508

### 核心创新

Entropy-guided adaptive inference; distinguishes Rigid Heads (near-zero entropy) vs Dynamic Heads (fluctuating entropy); head-specific sparsity patterns

### 关键数据

Adapts sparsity patterns to attention head characteristics

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★★ FTP: Efficient Prefilling for Long-Context LLM Inference via FFN Token Pruning

**iclr2025** | 评分 5.5 | arXiv:2412.11494

### 问题背景

Large Language Models (LLMs) have demonstrated remarkable performance across various NLP tasks, and have extended their capability to long-context scenarios. However, the increasing context length leads to longer inference time in both the prefilling and decoding stages.

### 核心创新

FTP: Fine-grained token-wise pruning with learnable router; search-based sparsity scheduler

### 关键数据

Outperforms BlockPruner and ShortGPT by ~10 points

### 技术标签

方法: 量化, 驱逐/剪枝 | 对象: kv, weight

---
## ★ GMSA: Enhancing Context Compression via Group Merging and Layer Semantic Alignment

**neurips2025** | 评分 4.0 | arXiv:2505.12215

### 问题背景

Large language models (LLMs) have achieved impressive performance in a variety of natural language processing (NLP) tasks. However, when applied to long-context scenarios, they face two challenges, i.e., low computational efficiency and much redundant information.

### 核心创新

GMSA: Group Merging + Layer Semantic Alignment; encoder-decoder context compression

### 关键数据

Improves over soft-prompt compression on long-context QA

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Graph-KV: Breaking Sequence via Injecting Structural Biases into Large Language Models

**neurips2025** | 评分 4.3 | arXiv:2506.07334

### 问题背景

Modern large language models (LLMs) are inherently auto-regressive, requiring input to be serialized into flat sequences regardless of their structural dependencies. This serialization hinders the modelâ€™s ability to leverage structural inductive biases, especially in tasks such as retrieval-augmen

### 核心创新

Graph-KV: Graph-structured block mask; structural inductive biases; message-passing-like attention

### 关键数据

Outperforms sequential encoding across various RAG benchmarks

### 技术标签

方法: 驱逐/剪枝, 稀疏化 | 对象: kv

---
##  GRKV: Global Regression for Training-Free KV Cache Compression

**arXiv 2026** | arXiv:2605.31105

### 核心创新

Global regression for span-based KV retention; preserves semantic coherence; training-free

### 关键数据

Training-free KV cache compression for long-context LLMs

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  HashAttention: Semantic Sparsity for Faster Inference

**icml2025** | arXiv:2412.14468

### 问题背景

Leveraging long contexts is crucial for advanced AI systems, but attention computation poses a scalability challenge. While scaled dot-product attention (SDPA) exhibits token sparsity, i.e.

### 核心创新

HashAttention: MIPS-based pivotal token identification; Hamming space encoding; bitwise operations

### 关键数据

4.3x attention latency reduction; 3.12x throughput improvement

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ HELM: Hyperbolic Large Language Models via Mixture-of-Curvature Experts

**neurips2025** | 评分 4.5 | arXiv:2505.24722

### 问题背景

Frontier large language models (LLMs) have shown great success in text modeling and generation tasks across domains. However, natural language exhibits inherent semantic hierarchies and nuanced geometric structure, which current LLMs do not capture completely owing to their reliance on Euclidean ope

### 核心创新

HELM: Fully hyperbolic LLM; HMLA (hyperbolic Multi-Head Latent Attention); Mixture-of-Curvature Experts

### 关键数据

Up to 4% gains over Euclidean LLaMA/DeepSeek architectures

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ Hierachical Balance Packing: Towards Efficient Supervised Fine-tuning for Long-Context LLM

**neurips2025** | 评分 3.8

### 问题背景

Training Long-Context Large Language Models (LLMs) is challenging, as hybrid training with long-context and short-context data often leads to workload imbalances. Existing works mainly use data packing to alleviate this issue, but fail to consider imbalanced attention computation and wasted communic

### 核心创新

Training Long-Context Large Language Models (LLMs) is challenging, as hybrid training with long-context and short-context data often leads to workload imbalances

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, activation, communication

---
## ★★ Hierarchy-Aided Sparse Attention For Fast LLMs Prefilling Inference

**iclr2025** | 评分 5.0

### 问题背景

Pre-filling Large Language Models (LLMs) with long-context inputs is computationally expensive due to the quadratic complexity of full attention. While global attention is essential during decoding, its importance diminishes during pre-filling, where the focus is on contextualizing tokens rather tha

### 核心创新

Pre-filling Large Language Models (LLMs) with long-context inputs is computationally expensive due to the quadratic complexity of full attention

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  HieraSparse: Hierarchical Semi-Structured Sparse KV Attention

**arXiv 2026** | arXiv:2604.16864

### 核心创新

Hierarchical semi-structured sparse KV attention; GPU sparse tensor cores; prefill and decode acceleration

### 关键数据

Long-context LLM deployment with reduced computational cost

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★ HiFC: High-efficiency Flash-based KV Cache Swapping for Scaling LLM Inference

**neurips2025** | 评分 4.2

### 问题背景

Largeâ€‘languageâ€‘model inference with long contexts often produces keyâ€“value (KV) caches whose footprint exceeds the capacity of highâ€‘bandwidth memory on a GPU. Prior LLM inference frameworks such as vLLM mitigate this pressure by swapping KV cache pages to host DRAM.

### 核心创新

Largeâ€‘languageâ€‘model inference with long contexts often produces keyâ€“value (KV) caches whose footprint exceeds the capacity of highâ€‘bandwidth memory on a GPU

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ Holistic Reasoning with Long-Context LMs: A Benchmark for Database Operations on Massive Textual Data

**iclr2025** | 评分 6.2 | arXiv:2507.04416

### 问题背景

The rapid increase in textual information means we need more efficient methods to sift through, organize, and understand it all. While retrieval-augmented generation (RAG) models excel in accessing information from large document collections, they struggle with complex tasks that require aggregation

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Homogeneous Keys, Heterogeneous Values: Exploiting Local KV Cache Asymmetry for Long-Context LLMs

**neurips2025** | 评分 4.0

### 问题背景

Recent advances in Large Language Models (LLMs) have highlighted the critical importance of extending context length, yet the quadratic complexity of attention mechanisms poses significant challenges for efficient long-context modeling. KV cache compression has emerged as a key approach to address t

### 核心创新

Recent advances in Large Language Models (LLMs) have highlighted the critical importance of extending context length, yet the quadratic complexity of attention mechanisms poses significant challenges ...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ HShare: Fast LLM Decoding by Hierarchical Key-Value Sharing

**iclr2025** | 评分 6.8

### 问题背景

The frequent retrieval of Key-Value (KV) cache data has emerged as a significant factor contributing to the inefficiency of the inference process in large language models. Previous research has demonstrated that a small subset of critical KV cache tokens largely influences attention outcomes, leadin

### 核心创新

The frequent retrieval of Key-Value (KV) cache data has emerged as a significant factor contributing to the inefficiency of the inference process in large language models

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ HSR-Enhanced Sparse Attention Acceleration

**iclr2025** | 评分 3.0 | arXiv:2507.04416

### 问题背景

Large Language Models (LLMs) have demonstrated remarkable capabilities across various applications, but their performance on long-context tasks is often limited by the computational complexity of attention mechanisms. This paper introduces a novel approach to accelerate attention computation in LLMs

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  IAM: Efficient Inference through Attention Mapping between Different-scale LLMs

**acl2025** | arXiv:2507.11953

### 问题背景

LLMs encounter significant challenges in resource consumption nowadays, especially with long contexts. Despite extensive efforts dedicate to enhancing inference efficiency, these methods primarily exploit internal sparsity within the models, without leveraging external information for optimization.

### 核心创新

IAM: Attention mapping between different-scale LLMs; dual benefits of acceleration and KV cache reduction

### 关键数据

15% prefill speedup; 22.1% KV cache reduction

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★★ Identify Critical KV Cache in LLM Inference from an Output Perturbation Perspective

**iclr2025** | 评分 5.8

### 问题背景

Large language models have driven numerous paradigm shifts in the field of natural language processing, achieving remarkable success in various real-world applications through scaling model size and leveraging long-sequence context reasoning. However, the transformer architecture, which relies on se

### 核心创新

Large language models have driven numerous paradigm shifts in the field of natural language processing, achieving remarkable success in various real-world applications through scaling model size and l...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ Improving Bilinear RNN with Closed-loop Control

**neurips2025** | 评分 5.0

### 问题背景

Recent efficient sequence modeling methods, such as Gated DeltaNet, TTT, and RWKV-7, have achieved performance improvements by supervising the recurrent memory management through the Delta learning rule. Unlike previous state-space models (e.g., Mamba) and gated linear attentions (e.g., GLA), these 

### 核心创新

Recent efficient sequence modeling methods, such as Gated DeltaNet, TTT, and RWKV-7, have achieved performance improvements by supervising the recurrent memory management through the Delta learning ru...

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ Improving Model Representation and Reducing KV Cache via Skip Connections with First Value Heads

**neurips2025** | 评分 4.2

### 问题背景

Transformer models have driven breakthroughs across various language tasks by their strong capability to learn rich contextual representations. Scaling them to improve representation, however, often demands substantial memory and compute costs, such as the Key-Value (KV) cache used during auto-regre

### 核心创新

Transformer models have driven breakthroughs across various language tasks by their strong capability to learn rich contextual representations

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
##  IndexMem: Learned KV-Cache Eviction with Latent Memory for Long-Context LLM Inference

**arXiv 2026** | arXiv:2605.25475

### 核心创新

Learned indexer predicting KV importance; latent memory for eviction policy; input-dependent token importance

### 关键数据

Learned KV-cache eviction vs heuristic methods

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  Inference-Friendly Models With MixAttention

**iclr2025** | 评分 2.0 | arXiv:2503.16428

### 问题背景

The size of the key-value (KV) cache plays a critical role in determining both the maximum context length and the number of concurrent requests supported during inference in modern language models. The KV cache size grows proportionally with the number of attention heads and the tokens processed, le

### 核心创新

XAttention: Antidiagonal scoring for block importance; block sparse attention; plug-and-play

### 关键数据

13.5x attention speedup; comparable to full attention

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ InfiniPot-V: Memory-Constrained KV Cache Compression for Streaming Video Understanding

**neurips2025** | 评分 4.2

### 问题背景

Modern multimodal large language models (MLLMs) can reason over hour-long video, yet their keyâ€“value (KV) cache grows linearly with timeâ€”quickly exceeding the fixed memory of phones, AR glasses, and edge robots. Prior compression schemes either assume the whole video and user query are available

### 核心创新

Continual Context Distillation (CCD); iterative compression with novel importance metrics; fixed memory constraint operation

### 关键数据

Outperforms models trained for long contexts in various NLP tasks

### 技术标签

方法: unclear | 对象: kv, weight

---
##  InfiniteICL: Breaking the Limit of Context Window Size via Long Short-term Memory Transformation

**acl2025** | arXiv:2504.01707

### 问题背景

In-context learning (ICL) is critical for large language models (LLMs), but its effectiveness is constrained by finite context windows, particularly in ultra-long contexts. To overcome this, we introduce **InfiniteICL**, a framework that parallels context and parameters in LLMs with short- and long-

### 核心创新

InfiniteICL: Long short-term memory transformation; context knowledge consolidation

### 关键数据

90% context length reduction; 103% average performance of full-context

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  Information-Aware KV Cache Compression for Long Reasoning

**arXiv 2026** | arXiv:2606.26875

### 核心创新

Information-theoretic signals for token importance: predictive uncertainty + token informativeness; beyond attention weights

### 关键数据

Improves KV cache compression for long reasoning

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ IntelLLM: Little Hints Make a Big Difference for LLM KV Cache Compression

**iclr2025** | 评分 3.0

### 问题背景

Large Language Models (LLMs) have demonstrated exceptional capabilities in integrating contextual knowledge, but their deployment is often constrained by the substantial computational resources required for long text sequences. To mitigate the inference time cost associated with attention mechanisms

### 核心创新

Large Language Models (LLMs) have demonstrated exceptional capabilities in integrating contextual knowledge, but their deployment is often constrained by the substantial computational resources requir...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, weight, activation

---
##  IsoQuant: Hardware-Aligned SO(4) Isoclinic Rotations for LLM KV Cache Compression

**arXiv 2026** | arXiv:2603.28430

### 核心创新

IsoQuant: SO(4) isoclinic rotations; quaternion algebra; blockwise rotation aligned with hardware; low-bit KV quantization

### 关键数据

Hardware-aligned KV cache compression

### 技术标签

方法: 量化 | 对象: kv

---
##  KV Cache Offloading for Context-Intensive Tasks

**arXiv 2026** | arXiv:2604.08426

### 核心创新

KV cache offloading for context-intensive tasks; memory-latency tradeoff optimization

### 关键数据

Reduced memory footprint and inference latency

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★★ KV-Dict: Sparse KV Cache Compression with Universal Dictionaries

**iclr2025** | 评分 5.2

### 问题背景

Transformer has become the de facto architecture for Large Language Models (LLMs), yet its substantial memory required for long contexts make it costly to deploy. Managing the memory usage of the key-value (KV) cache during inference has become a pressing challenge, as the cache grows with both mode

### 核心创新

Transformer has become the de facto architecture for Large Language Models (LLMs), yet its substantial memory required for long contexts make it costly to deploy

### 关键数据

See abstract for details

### 技术标签

方法: 量化, 稀疏化 | 对象: kv, weight, activation

---
## ★ KVFlow: Efficient Prefix Caching for Accelerating LLM-Based Multi-Agent Workflows

**neurips2025** | 评分 4.0 | arXiv:2507.07400

### 问题背景

Large language model (LLM) based agentic workflows have become a popular paradigm for coordinating multiple specialized agents to solve complex tasks. To improve serving efficiency, existing LLM systems employ prefix caching to reuse key-value (KV) tensors corresponding to agents' fixed prompts, the

### 核心创新

KVFlow: Agent Step Graph; steps-to-execution value; fine-grained eviction; KV prefetching

### 关键数据

1.83x single workflow speedup; 2.19x concurrent workflow speedup

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, activation

---
## ★ KVLink: Accelerating Large Language Models via Efficient KV Cache Reuse

**neurips2025** | 评分 4.2 | arXiv:2502.16002

### 问题背景

We describe KVLink, an approach for efficient key-value (KV) cache reuse in large language models (LLMs). In many LLM applications, different inputs can share overlapping context, such as the same retrieved document appearing in multiple queries.

### 核心创新

KVLink: Precomputed KV cache reuse; positional embedding adjustment; trainable special tokens for cross-document attention

### 关键数据

96% TTFT reduction; 4% QA accuracy improvement

### 技术标签

方法: 通用优化 | 对象: kv

---
##  KVPR: Efficient LLM Inference with I/O-Aware KV Cache Partial Recomputation

**acl2025** | arXiv:2411.17089

### 问题背景

Inference for Large Language Models (LLMs) is computationally demanding. To reduce the cost of auto-regressive decoding, Key-Value (KV) cache is used to store intermediate activations, which significantly lowers the computational overhead for token generation.

### 核心创新

KVPR: I/O-aware partial recomputation; CPU first transfers partial activations; GPU recomputes while remaining KV transfers

### 关键数据

35.8% latency reduction; 46.2% throughput improvement

### 技术标签

方法: 通用优化 | 对象: kv, activation

---
##  KVServe: Service-Aware KV Cache Compression for Communication-Efficient Disaggregated LLM Serving

**SIGCOMM 2026** | arXiv:2605.13734

### 核心创新

KVServe: Service-aware adaptive KV compression; Bayesian Profiling Engine; 3D Pareto candidate set; Service-Aware Online Controller with analytical latency model

### 关键数据

9.13x JCT speedup in PD-separated serving; 32.8x TTFT reduction in KV-disaggregated serving (SIGCOMM 2026)

### 技术标签

方法: 驱逐/剪枝, 量化 | 对象: kv, communication

---
## ★ KVSharer: Efficient Inference via Layer-Wise Dissimilar KV Cache Sharing

**iclr2025** | 评分 4.2 | arXiv:2410.18517

### 问题背景

The development of large language models (LLMs) has significantly expanded model sizes, resulting in substantial GPU memory requirements during inference. The key and value storage of the attention map in the KV (key-value) cache accounts for more than 80\% of this memory consumption.

### 核心创新

KVSharer: Layer-wise KV sharing between dissimilar layers

### 关键数据

30% KV computation reduction; 1.3x generation acceleration

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  KVTuner: Sensitivity-Aware Layer-Wise Mixed-Precision KV Cache Quantization for Efficient and Nearly Lossless LLM Infere

**icml2025** | arXiv:2502.04420

### 问题背景

KV cache quantization can improve Large Language Models (LLMs) inference throughput and latency in long contexts and large batch-size scenarios while preserving LLMs effectiveness. However, current methods have three unsolved issues: overlooking layer-wise sensitivity to KV cache quantization, high 

### 核心创新

KVTuner: Sensitivity-aware layer-wise mixed precision; intra-layer KV pruning; inter-layer clustering

### 关键数据

Nearly lossless 3.25-bit for Llama-3.1-8B; 21.25% throughput improvement over KIVI

### 技术标签

方法: 量化, 驱逐/剪枝 | 对象: kv, weight

---
##  LaCache: Ladder-Shaped KV Caching for Efficient Long-Context Modeling of Large Language Models

**icml2025** | arXiv:2507.14204

### 问题背景

Recent advancements in Large Language Models (LLMs) have spurred interest in numerous applications requiring robust long-range capabilities, essential for processing extensive input contexts and continuously generating extended outputs. As sequence lengths increase, the number of Key-Value (KV) pair

### 核心创新

LaCache: Ladder-shaped KV cache pattern; iterative compaction mechanism; dynamic compression based on token distance

### 关键数据

Enhanced long-range capabilities with fixed cache budget

### 技术标签

方法: 通用优化 | 对象: kv

---
##  LAMB: A Training-Free Method to Enhance the Long-Context Understanding of SSMs via Attention-Guided Token Filtering

**acl2025**

### 问题背景

State space models (SSMs) achieve efficient sub-quadratic compute complexity but often exhibit significant performance drops as context length increases. Recent work attributes this deterioration to an exponential decay in hidden-state memory.

### 核心创新

State space models (SSMs) achieve efficient sub-quadratic compute complexity but often exhibit significant performance drops as context length increases

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ Language Repository for Long Video Understanding

**iclr2025** | 评分 5.5

### 问题背景

Language has become a prominent modality in computer vision with the rise of LLMs. Despite supporting long context-lengths, their effectiveness in handling long-term information gradually declines with input length.

### 核心创新

Language has become a prominent modality in computer vision with the rise of LLMs

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★ Latte: Latent Attention for Linear Time Transformers

**iclr2025** | 评分 3.5

### 问题背景

The time complexity of the standard attention mechanism in transformers scales quadratically with sequence length. We propose a probabilistic framework for attention, enabling us to derive a novel low-rank linear re-parameterisation of both bidirectional and causal cases, based on defining a latent 

### 核心创新

The time complexity of the standard attention mechanism in transformers scales quadratically with sequence length

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ LaViDa: A Large Diffusion Model for Vision-Language Understanding

**neurips2025** | 评分 4.2 | arXiv:2505.16839

### 问题背景

Modern Vision-Language Models (VLMs) can solve a wide range of tasks requiring visual reasoning. In real-world scenarios, desirable properties for VLMs include fast inference and controllable generation (e.g., constraining outputs to adhere to a desired format).

### 核心创新

LaViDa: Diffusion-based VLM; complementary masking; prefix KV cache; timestep shifting

### 关键数据

+4.1 CIDEr over Open-LLaVa-Next-8B; 1.92x speedup

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Learned Prefix Caching for Efficient LLM Inference

**neurips2025** | 评分 4.0

### 问题背景

Prefix caching is a key technique for reducing Large Language Model (LLM) inference costs. However, the prevalent least-recently-used (LRU) eviction algorithm has a large gap to the optimal algorithm.

### 核心创新

Prefix caching is a key technique for reducing Large Language Model (LLM) inference costs

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★★ Learning Harmonized Representations for Speculative Sampling

**iclr2025** | 评分 7.0

### 问题背景

Speculative sampling is a promising approach to accelerate the decoding stage for Large Language Models (LLMs). Recent advancements that leverage target LLM's contextual information, such as hidden states and KV cache, have shown significant practical improvements.

### 核心创新

Speculative sampling is a promising approach to accelerate the decoding stage for Large Language Models (LLMs)

### 关键数据

See abstract for details

### 技术标签

方法: 蒸馏 | 对象: kv

---
##  Learning Long-Context Diffusion Policies via Past-Token Prediction

**corl2025** | arXiv:2505.09561

### 问题背景

Reasoning over long sequences of observations and actions is essential for many robotic tasks. Yet, learning effective long-context policies from demonstrations remains challenging.

### 核心创新

PTP: Auxiliary past-token prediction; multistage training; cached long-context embeddings

### 关键数据

3x policy improvement; 10x+ training acceleration

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Learning to Focus: Causal Attention Distillation via Gradientâ€Guided Token Pruning

**neurips2025** | 评分 4.0 | arXiv:2602.02180

### 问题背景

Large language models (LLMs) have demonstrated significant improvements in contextual understanding. However, their ability to attend to truly critical information during long-context reasoning and generation still falls behind the pace.

### 核心创新

STILL: Self-Saliency Score for token selection; Norm-Preserved Feature Map; chunk-wise parallelization

### 关键数据

86.2% improvement over prior linearized attention on long-context

### 技术标签

方法: 驱逐/剪枝, 蒸馏 | 对象: kv, weight, activation

---
## ★★ Let the Code LLM Edit Itself When You Edit the Code

**iclr2025** | 评分 5.5

### 问题背景

In this work, we investigate a typical scenario in code generation where a developer edits existing code in real time and requests a code assistant, e.g., a large language model, to re-predict the next token or next line on the fly. Naively, the LLM needs to re-encode the entire KV cache to provide 

### 核心创新

In this work, we investigate a typical scenario in code generation where a developer edits existing code in real time and requests a code assistant, e

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ LevAttention: Time, Space and Streaming Efficient Algorithm for Heavy Attentions

**iclr2025** | 评分 5.8 | arXiv:2410.05462

### 问题背景

A central problem related to transformers can be stated as follows: given two $n \times d$ matrices $Q$ and $K$, and a non-negative function $f$, define the matrix $A$ as follows: (1) apply the function $f$ to each entry of the $n \times n$ matrix $Q K^T$, and then (2) normalize each of the row sums

### 核心创新

LevAttention: Time/space/streaming efficient heavy attention; leverage scores for universal key set; identifies important keys

### 关键数据

Linear time for large attention scores

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ LION: A bidirectional framework that trains like a Transformer and infers like an RNN

**iclr2025** | 评分 4.8 | arXiv:2502.16249

### 问题背景

We introduce LION, a novel sequence-to-sequence framework that unifies the bidirectionality and parallelized training of Transformers with the fast inference of recurrent neural networks. LION is built upon a mathematical formulation where full kernelized attention with a learnable mask is efficient

### 核心创新

LION: Bidirectional Linear Transformer framework; full/RNN/chunkwise parallel forms

### 关键数据

Matches softmax Transformers; faster training (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Live2Diff: Live Stream Translation via Uni-directional Attention in Video Diffusion Models

**iclr2025** | 评分 4.7 | arXiv:2407.08701

### 问题背景

Large Language Models have shown remarkable efficacy in generating streaming data such as text and audio, thanks to their temporally uni-directional attention mechanism, which models correlations between the current token and *previous* tokens. However, video streaming remains much less explored, de

### 核心创新

Live2Diff: First uni-directional temporal attention for video diffusion; KV-cache + pipelining

### 关键数据

Interactive framerates for streaming video

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ LiveStar: Live Streaming Assistant for Real-World Online Video Understanding

**neurips2025** | 评分 3.8 | arXiv:2511.05299

### 问题背景

Despite significant progress in Video Large Language Models (Video-LLMs) for offline video understanding, existing online Video-LLMs typically struggle to simultaneously process continuous frame-by-frame inputs and determine optimal response timing, often compromising real-time responsiveness and na

### 核心创新

LiveStar: Adaptive streaming decoding; peak-end memory compression; streaming KV cache

### 关键数据

19.5% semantic improvement; 1.53x faster inference (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ LogQuant: Log-Distributed 2-Bit Quantization of KV Cache with Superior Accuracy Preservation

**iclr2025** | 评分 4.0 | arXiv:2503.19950

### 问题背景

We introduce LogQuant, a groundbreaking 2-bit quantization technique for KV Cache in large language model (LLM) inference, delivering substantial memory savings while preserving superior performance. Previous methods either assume that later tokens are more important or attempt to predict important 

### 核心创新

LogQuant: Log-based filtering for 2-bit KV quantization; selective compression across context

### 关键数据

25% throughput increase; 60% batch size boost; 40-200% accuracy improvement on Math/Code

### 技术标签

方法: 量化 | 对象: kv, communication

---
##  LongAttn: Selecting Long-context Training Data via Token-level Attention

**acl2025** | arXiv:2502.16860

### 问题背景

With the development of large language models (LLMs), there has been an increasing need for significant advancements in handling long contexts. To enhance long-context capabilities, constructing high-quality training data with **long-range dependencies** is crucial.

### 核心创新

LongAttn: Token-level attention for long-range dependency measurement

### 关键数据

Efficient long-context data selection

### 技术标签

方法: 量化 | 对象: kv

---
##  Long-context Extrapolation via Periodic Extension

**iclr2025** | 评分 2.5

### 问题背景

Long-context extrapolation aims to extend the contextual window of large language models to process more contextual information, which is widely adopted in industrial applications. Current mainstream solutions involve increasing the rotation base of RoPE to varying degrees or introducing optimizatio

### 核心创新

Long-context extrapolation aims to extend the contextual window of large language models to process more contextual information, which is widely adopted in industrial applications

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ LongLLaVA: Scaling Multi-modal LLMs to 1000 Images Efficiently via a Hybrid Architecture

**iclr2025** | 评分 5.8 | arXiv:2409.02889

### 问题背景

Expanding the long-context capabilities of Multi-modal Large Language Models (MLLMs) is crucial for video understanding, high-resolution image understanding, and multi-modal agents. This involves a series of systematic optimizations, including model architecture, data construction and training strat

### 核心创新

LongLLaVA: Hybrid Mamba+Transformer; temporal/spatial data construction; progressive training

### 关键数据

~1000 images on single A100 80GB

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ LongMamba: Enhancing Mamba's Long-Context Capabilities via Training-Free Receptive Field Enlargement

**iclr2025** | 评分 6.8

### 问题背景

State space models (SSMs) have emerged as an efficient alternative to Transformer models for language modeling, offering linear computational complexity and constant memory usage as context length increases. However, despite their efficiency in handling long contexts, recent studies have shown that 

### 核心创新

Training-free technique categorizing hidden channels into local and global; token filtering for global channels to prevent hidden state memory decay

### 关键数据

Significantly extends operational range of Mamba models without additional training

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ LongVILA: Scaling Long-Context Visual Language Models for Long Videos

**iclr2025** | 评分 6.7

### 问题背景

Long-context capability is critical for multi-modal foundation models, especially for long video understanding. We introduce LongVILA, a full-stack solution for long-context visual-language models by co-designing the algorithm and system.

### 核心创新

Full-stack long-context VLM with MM-SP parallelism; 2M context training on 256 GPUs; long video extension and fine-tuning stages

### 关键数据

99.8% accuracy in 6,000-frame needle-in-a-haystack; 2.1x-5.7x faster than ring-style SP

### 技术标签

方法: 通用优化 | 对象: kv, activation, communication

---
## ★ LongVPO: From Anchored Cues to Self-Reasoning for Long-Form Video Preference Optimization

**neurips2025** | 评分 4.0 | arXiv:2602.02341

### 问题背景

We present LongVPO, a novel twoâ€‘stage Direct Preference Optimization framework that enables shortâ€‘context visionâ€‘language models to robustly understand ultraâ€‘long videos without any longâ€‘video annotations. In Stage 1, we synthesize preference triples by anchoring questions to individual sh

### 核心创新

LongVPO: Two-stage DPO; anchored clip preference triples; recursive captioning pipeline

### 关键数据

Outperforms SOTA on long-video benchmarks (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ LoRA-Gen: Specializing Language Model via Online LoRA Generation

**iclr2025** | 评分 6.0 | arXiv:2507.04416

### 问题背景

Recent advances have highlighted the benefits of scaling language models to enhance performance across a wide range of NLP tasks. However, these approaches still face limitations in effectiveness and efficiency when applied to domain-specific tasks, particularly for small edge-side models.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 低秩 | 对象: kv, weight

---
##  LoRA-Gen: Specializing Large Language Model via Online LoRA Generation

**icml2025** | arXiv:2507.04416

### 问题背景

Recent advances have highlighted the benefits of scaling language models to enhance performance across a wide range of NLP tasks. However, these approaches still face limitations in effectiveness and efficiency when applied to domain-specific tasks, particularly for small edge-side models.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★★ LoRC: Low-Rank Compression for LLMs KV Cache with a Progressive Compression Strategy

**iclr2025** | 评分 5.0

### 问题背景

The Key-Value (KV) cache is a crucial component in serving transformer-based autoregressive large language models (LLMs), enabling faster inference by storing previously computed KV vectors. However, its memory consumption scales linearly with sequence length and batch size, posing a significant bot

### 核心创新

Low-rank approximation of KV weight matrices; plug-in integration without retraining; layerwise sensitivity adjustment; progressive compression strategy

### 关键数据

Significantly reduces GPU memory footprint while maintaining performance

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ LSH Tells You What To Discard: An Adaptive Locality-Sensitive Strategy for KV Cache Compression

**iclr2025** | 评分 3.8 | arXiv:2507.04416

### 问题背景

Transformer-based large language models (LLMs) use the key-value (KV) cache to significantly accelerate inference by storing the key and value embeddings of past tokens. However, this cache consumes significant GPU memory.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ Mamba Goes HoME: Hierarchical Soft Mixture-of-Experts for 3D Medical Image Segmentation

**neurips2025** | 评分 4.0 | arXiv:2312.00752

### 问题背景

In recent years, artificial intelligence has significantly advanced medical image segmentation. Nonetheless, challenges remain, including efficient 3D medical image processing across diverse modalities and handling data variability.

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: 通用优化 | 对象: kv, communication

---
## ★ MambaMatch: Learning Two-View Correspondences with Selective State Spaces

**iclr2025** | 评分 4.8 | arXiv:2312.00752

### 问题背景

Two-view correspondence learning aims to discern true and false correspondences between image pairs by recognizing their underlying different information. Previous methods either treat the information equally or fail to discard the superfluous information of false correspondences, tending to be inva

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★ Many-Shot In-Context Learning in Multimodal Foundation Models

**iclr2025** | 评分 4.5

### 问题背景

Large language models are well-known to be effective at few-shot in-context learning (ICL). Recent advancements in multimodal foundation models have enabled unprecedentedly long context windows, presenting an opportunity to explore their capability to perform ICL with many more demonstrating example

### 核心创新

Large language models are well-known to be effective at few-shot in-context learning (ICL)

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ Markovian Transformers for Informative Language Modeling

**iclr2025** | 评分 6.8 | arXiv:2404.18988

### 问题背景

Chain-of-Thought (CoT) reasoning holds great promise for explaining language model outputs, but recent studies have highlighted significant challenges in its practical application for interpretability. We propose to address this issue by making CoT causally essential to prediction through two key co

### 核心创新

Markovian autoencoder-style reasoning bottleneck; bounded-length CoT; GRPO policy gradient

### 关键数据

GSM8K: 19.6% to 57.1%; ARC: 36.1% to 79.9%

### 技术标签

方法: 通用优化 | 对象: kv, activation

---
##  Massive Values in Self-Attention Modules are the Key to Contextual Knowledge Understanding

**icml2025** | arXiv:2502.01563

### 问题背景

Large language models (LLMs) have achieved remarkable success in contextual knowledge understanding. In this paper, we show for the first time that these concentrated massive values consistently emerge in specific regions of attention queries (Q) and keys (K) while not having such patterns in values

### 核心创新

Identifies massive values in Q/K caused by RoPE; critical for contextual (not parametric) knowledge

### 关键数据

Ignoring massive values degrades contextual understanding (ICML 2025)

### 技术标签

方法: 量化 | 对象: kv, weight

---
## ★★ MatMamba: A Matryoshka State Space Model

**iclr2025** | 评分 5.8 | arXiv:2312.00752

### 问题背景

State Space Models (SSMs) like Mamba2 are a promising alternative to Transformers, with faster theoretical training and inference times -- especially for long context lengths. Recent work on Matryoshka Representation Learning -- and its application to Transformer backbones in works like MatFormer --

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ Memory-Efficient Visual Autoregressive Modeling with Scale-Aware KV Cache Compression

**neurips2025** | 评分 4.4

### 问题背景

Visual Autoregressive (VAR) modeling has garnered significant attention for its innovative next-scale prediction approach, which yields substantial improvements in efficiency, scalability, and zero-shot generalization. Nevertheless, the coarse-to-fine methodology inherent in VAR results in exponenti

### 核心创新

Visual Autoregressive (VAR) modeling has garnered significant attention for its innovative next-scale prediction approach, which yields substantial improvements in efficiency, scalability, and zero-sh...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
##  MemoSight: Unifying Context Compression and Multi Token Prediction for Reasoning Acceleration

**arXiv 2026** | arXiv:2604.14889

### 核心创新

MemoSight: Unifies context compression + multi-token prediction (MTP); shared minimalist design with special tokens; token-specific positional layouts

### 关键数据

66% KV cache reduction; 56% inference speedup; <3% accuracy drop on reasoning benchmarks

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ MIR-Bench: Can Your LLM Recognize Complicated Patterns via Many-Shot In-Context Reasoning?

**neurips2025** | 评分 4.5 | arXiv:2502.09933

### 问题背景

The ability to recognize patterns from examples and apply them to new ones is a primal ability for general intelligence, and is widely studied by psychology and AI researchers. Many benchmarks have been proposed to measure such ability for Large Language Models (LLMs); however, they focus on few-sho

### 核心创新

MIR-Bench: Many-shot pattern recognition benchmark; inductive vs transductive reasoning

### 关键数据

First many-shot ICL reasoning benchmark (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Mixtures of Subspaces for Bandwidth Efficient Context Parallel Training

**neurips2025** | 评分 4.0 | arXiv:2606.16384

### 问题背景

Pretraining language models with extended context windows enhances their ability to leverage rich information during generation. Existing methods split input sequences into chunks, broadcast them across multiple devices, and compute attention block by block which incurs significant communication ove

### 核心创新

Low-rank activation compression; mixture-of-subspaces reparameterization; >95% compression

### 关键数据

100K+ tokens on 300Mbps networks; matches 100Gbps centralized convergence

### 技术标签

方法: 低秩 | 对象: kv, weight, activation

---
##  M-LLM Based Video Frame Selection for Efficient Video Understanding

**cvpr2025**

### 问题背景

Recent advances in Multi-Modal Large Language Models (M-LLMs) show promising results in video reasoning. Popular Multi-Modal Large Language Model (M-LLM) frameworks usually apply naive uniform sampling to reduce the number of video frames that are fed into an M-LLM, particularly for long context vid

### 核心创新

Recent advances in Multi-Modal Large Language Models (M-LLMs) show promising results in video reasoning

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  MMInference: Accelerating Pre-filling for Long-Context Visual Language Models via Modality-Aware Permutation Sparse Atte

**icml2025** | arXiv:2504.16083

### 问题背景

The integration of long-context capabilities with visual understanding unlocks unprecedented potential for Vision Language Models (VLMs). However, the quadratic attention complexity during the pre-filling phase remains a significant obstacle to real-world deployment.

### 核心创新

MMInference: Modality-aware permutation sparse attention; accelerates prefilling

### 关键数据

8.3x prefill speedup at 1M tokens (ICML 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★★ MoA: Mixture of Sparse Attention for Automatic Large Language Model Compression

**iclr2025** | 评分 5.5

### 问题背景

Sparse attention can effectively mitigate the significant memory and throughput demands of Large Language Models (LLMs) in long contexts. Existing methods typically employ a uniform sparse attention mask, applying the same sparse pattern across different attention heads and input lengths.

### 核心创新

Sparse attention can effectively mitigate the significant memory and throughput demands of Large Language Models (LLMs) in long contexts

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, weight

---
##  MobiLoRA: Accelerating LoRA-based LLM Inference on Mobile Devices via Context-aware KV Cache Optimization

**acl2025** | arXiv:2507.04416

### 问题背景

Deploying large language models (LLMs) with low-rank adaptation (LoRA) on mobile devices is promising due to their capability to complete diverse domain-specific tasks while ensuring privacy and accessibility. In this paper, we introduce MobiLoRA to accelerate LoRA-based LLM inference on mobile devi

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ Model Tells You Where to Merge: Adaptive KV Cache Merging for LLMs on Long-Context Tasks

**iclr2025** | 评分 4.3

### 问题背景

How to efficiently serve Large Language Models (LLMs) has become a pressing issue because of their huge computational cost in their autoregressive generation process. To mitigate computational costs, LLMs often employ the KV Cache technique to improve the generation speed.

### 核心创新

How to efficiently serve Large Language Models (LLMs) has become a pressing issue because of their huge computational cost in their autoregressive generation process

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, weight, activation

---
##  Moment-KV: Momentum-Based Decode-Time KV Cache Compression

**arXiv 2026** | arXiv:2605.29873

### 核心创新

Moment-KV: Momentum-driven temporal attention aggregation; models token importance as evolving state with decay; captures long-term influence + recent relevance

### 关键数据

2.3-3.2% improvement in generation fidelity for long-generation tasks

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  MoQAE: Mixed-Precision Quantization for Long-Context LLM Inference via Mixture of Quantization-Aware Experts

**acl2025** | arXiv:2506.07533

### 问题背景

One of the primary challenges in optimizing large language models (LLMs) for long-context inference lies in the high memory consumption of the Key-Value (KV) cache. Existing approaches, such as quantization, have demonstrated promising results in reducing memory usage.

### 核心创新

MoQAE: Mixture of quantization-aware experts; chunk-based routing; router-only fine-tuning

### 关键数据

Outperforms SOTA KV quantization (ACL 2025)

### 技术标签

方法: 量化 | 对象: kv, weight, communication

---
## ★★ MPCache: MPC-Friendly KV Cache Eviction for Efficient Private LLM Inference

**iclr2025** | 评分 5.0 | arXiv:2501.06807

### 问题背景

Private LLM inference based on multi-party computation (MPC) offers cryptographically-secure protection for both user prompt and proprietary model weights. However, it suffers from large latency overhead for long input sequences.

### 核心创新

MPCache: MPC-friendly KV eviction; static + dynamic selection; hierarchical clustering; cross-layer index sharing

### 关键数据

1.8-2.01x decoding latency reduction; 3.39-8.37x communication reduction

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  MSA: Memory Sparse Attention for Efficient End-to-End Memory Model Scaling to 100M Tokens

**arXiv 2026** | arXiv:2603.23516

### 核心创新

MSA: Memory Sparse Attention; scalable sparse attention + document-wise RoPE; linear complexity; KV cache compression + Memory Parallel

### 关键数据

<9% degradation from 16K to 100M tokens; 100M-token inference on 2xA800 GPUs

### 技术标签

方法: 稀疏化 | 对象: kv

---
## ★ Multi-Agent Decision S4: Leveraging State Space Models for Offline Multi-Agent Reinforcement Learning

**iclr2025** | 评分 4.2

### 问题背景

Goal-conditioned sequence-based supervised learning with transformers has shown promise in offline reinforcement learning (RL) for single-agent settings. However, extending these methods to offline multi-agent RL (MARL) remains challenging.

### 核心创新

Goal-conditioned sequence-based supervised learning with transformers has shown promise in offline reinforcement learning (RL) for single-agent settings

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight, activation

---
## ★ Multi-head Temporal Latent Attention

**neurips2025** | 评分 4.2

### 问题背景

While Transformer self-attention offers strong parallelism, the  Key-Value (KV) cache grows linearly with sequence length and becomes a bottleneck for inference efficiency. Multi-head latent attention was recently developed to compress the KV cache into a low-rank latent space.

### 核心创新

While Transformer self-attention offers strong parallelism, the  Key-Value (KV) cache grows linearly with sequence length and becomes a bottleneck for inference efficiency

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv, weight

---
##  Multi-matrix Factorization Attention

**acl2025**

### 问题背景

We propose novel attention architectures, Multi-matrix Factorization Attention (MFA) and MFA-Key-Reuse (MFA-KR). Existing variants for standard Multi-Head Attention (MHA), including SOTA methods like MLA, fail to maintain as strong performance under stringent Key-Value cache (KV cache) constraints.

### 核心创新

Dictionary learning for attention projection matrices; shared dictionary atoms across layers; 66.7% attention parameter reduction

### 关键数据

Better benchmark accuracy and perplexity than GQA and low-rank baselines at comparable parameter budgets

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ Multimodal Instruction Tuning with Hybrid State Space Models

**iclr2025** | 评分 3.4

### 问题背景

Handling lengthy context is crucial for enhancing the recognition and understanding capabilities of multimodal large language models (MLLMs) in applications such as processing high-resolution images or high frame rate videos. The rise in image resolution and frame rate substantially increases comput

### 核心创新

Handling lengthy context is crucial for enhancing the recognition and understanding capabilities of multimodal large language models (MLLMs) in applications such as processing high-resolution images o...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Multipole Attention for Efficient Long Context Reasoning

**neurips2025** | 评分 4.2 | arXiv:2506.13059

### 问题背景

Large Reasoning Models (LRMs) have shown promising accuracy improvements on complex problem-solving tasks. While these models have attained high accuracy by leveraging additional computation at test time, they need to generate long chain-of-thought reasoning in order to think before answering, which

### 核心创新

Multipole Attention: Semantic key clustering; centroid-based importance; approximate representations

### 关键数据

4.5x attention speedup for long-context reasoning

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ Neural Attention Search

**neurips2025** | 评分 4.2

### 问题背景

We present Neural Attention Search (NAtS), an end-to-end learnable sparse transformer that automatically evaluates the importance of each token within a sequence and determines if the corresponding token can be dropped after several steps. To this end, we design a search space that contains three to

### 核心创新

We present Neural Attention Search (NAtS), an end-to-end learnable sparse transformer that automatically evaluates the importance of each token within a sequence and determines if the corresponding to...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, weight

---
## ★ NSNQuant: A Double Normalization Approach for Calibration-Free Low-Bit Vector Quantization of KV Cache

**neurips2025** | 评分 4.0 | arXiv:2507.04416

### 问题背景

Large Language Model (LLM) inference is typically memory-intensive, especially when processing large batch sizes and long sequences, due to the large size of key-value (KV) cache. Vector Quantization (VQ) is recently adopted to alleviate this issue, but we find that the existing approach is suscepti

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 量化 | 对象: kv

---
## ★★ OmniKV: Dynamic Context Selection for Efficient Long-Context LLMs

**iclr2025** | 评分 6.0

### 问题背景

During the inference phase of Large Language Models (LLMs) with long context, a substantial portion of GPU memory is allocated to the KV cache, with memory usage increasing as the sequence length grows. To mitigate the GPU memory footprint associate with KV cache, some previous studies have discarde

### 核心创新

During the inference phase of Large Language Models (LLMs) with long context, a substantial portion of GPU memory is allocated to the KV cache, with memory usage increasing as the sequence length grow...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  OmniMem: Perturbation-aware Memory Compression for Streaming Audio-Visual LLMs

**arXiv 2026** | arXiv:2606.07577

### 核心创新

OmniMem: Perturbation-aware memory selection; modality-aware allocation for visual/audio contexts; budget-aware fine-tuning

### 关键数据

2-4% absolute accuracy improvement over baselines; 1-2% additional gain with fine-tuning

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★★ On Exploring Visual Attention Shrinking for Accelerating VLMs for Video Understanding

**iclr2025** | 评分 5.8 | arXiv:2507.04416

### 问题背景

Vision-language models (VLMs) have shown promise in a variety of challenging video comprehension tasks. VLMs typically extract frames from the source video and take the corresponding encoded visual tokens as input.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  One Pass Streaming Algorithm for Super Long Token Attention Approximation in Sublinear Space

**iclr2025** | 评分 2.3 | arXiv:2311.14652

### 问题背景

Attention computation takes both the time complexity of $O(n^2)$ and the space complexity of $O(n^2)$ simultaneously, which makes deploying Large Language Models (LLMs) in streaming applications that involve long contexts requiring substantial computational resources. In recent OpenAI DevDay (Nov 6,

### 核心创新

Streaming attention approximation in sublinear space; sketch matrices; polynomial method

### 关键数据

Sublinear space o(n); near-constant memory for super-long tokens

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ One Token per Highly Selective Frame: Towards Extreme Compression for Long Video Understanding

**neurips2025** | 评分 4.7

### 问题背景

Long video understanding is inherently challenging for vision-language models (VLMs) because of the extensive number of frames. With each video frame typically expanding into tens or hundreds of tokens, the limited context length of large language models (LLMs) forces the VLMs to perceive the frames

### 核心创新

Long video understanding is inherently challenging for vision-language models (VLMs) because of the extensive number of frames

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  One-Minute Video Generation with Test-Time Training

**cvpr2025** | arXiv:2507.04416

### 问题背景

Transformers today still struggle to generate one-minute videos because self-attention layers are inefficient for long context. Alternatives such as Mamba layers struggle to produce coherent scenes because their hidden states are small and less expressive.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  On-the-Fly Adaptive Distillation of Transformer to Dual-State Linear Attention for Long-Context LLM Serving

**icml2025** | arXiv:2602.02180

### 问题背景

Large language models (LLMs) excel at capturing global token dependencies via self-attention but face prohibitive compute and memory costs on lengthy inputs. While sub-quadratic methods (e.g., linear attention) can reduce these costs, they often degrade accuracy due to overemphasizing recent tokens.

### 核心创新

STILL: Self-Saliency Score for token selection; Norm-Preserved Feature Map; chunk-wise parallelization

### 关键数据

86.2% improvement over prior linearized attention on long-context

### 技术标签

方法: 蒸馏 | 对象: kv

---
##  OrcaLoca: An LLM Agent Framework for Software Issue Localization

**icml2025**

### 问题背景

Recent developments in Large Language Model (LLM) agents are revolutionizing Autonomous Software Engineering (ASE), enabling automated coding, problem fixes, and feature improvements. However, localization -- precisely identifying software problems by navigating to relevant code sections -- remains 

### 核心创新

Recent developments in Large Language Model (LLM) agents are revolutionizing Autonomous Software Engineering (ASE), enabling automated coding, problem fixes, and feature improvements

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝, 低秩 | 对象: kv, weight

---
## ★ Overcoming Long Context Limitations of State Space Models via Context Dependent Sparse Attention

**neurips2025** | 评分 4.0

### 问题背景

Efficient long-context modeling remains a critical challenge for natural language processing (NLP), as the time complexity of the predominant Transformer architecture scales quadratically with the sequence length. While state-space models (SSMs) offer alternative sub-quadratic solutions, they strugg

### 核心创新

Efficient long-context modeling remains a critical challenge for natural language processing (NLP), as the time complexity of the predominant Transformer architecture scales quadratically with the seq...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  ParallelComp: Parallel Long-Context Compressor for Length Extrapolation

**icml2025** | arXiv:2502.14317

### 问题背景

Extrapolating ultra-long contexts (text length >128K) remains a major challenge for large language models (LLMs), as most training-free extrapolation methods are not only severely limited by memory bottlenecks, but also suffer from the attention sink, which restricts their scalability and effectiven

### 核心创新

ParallelComp: Chunk-based parallel KV eviction; attention bias analysis (sink/recency/middle)

### 关键数据

8K to 128K extrapolation; 91.17% of GPT-4; 23.5x prefill acceleration (ICML 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  PersonaX: A Recommendation Agent-Oriented User Modeling Framework for Long Behavior Sequence

**acl2025** | arXiv:2503.02398

### 问题背景

User profile embedded in the prompt template of personalized recommendation agents play a crucial role in shaping their decision-making process. High-quality user profiles are essential for aligning agent behavior with real user interests.

### 核心创新

PersonaX: Sub behavior sequence selection; offline multi-persona profiling; decoupled inference

### 关键数据

3-11% AgentCF; 10-50% Agent4Rec improvement (ACL 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ PrefixKV: Adaptive Prefix KV Cache is What Vision Instruction-Following Models Need for Efficient Generation

**neurips2025** | 评分 4.2 | arXiv:2412.03409

### 问题背景

Recently, large vision-language models (LVLMs) have rapidly gained popularity for their strong generation and reasoning capabilities given diverse multimodal inputs. However, these models incur significant computational and memory overhead during inference, which greatly hinders the efficient deploy

### 核心创新

PrefixKV: Adaptive prefix KV cache for VLMs; binary search for layer-wise retention; preserves maximum contextual information

### 关键数据

Optimal KV cache sizes for vision instruction-following

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Preserving Large Activations: The Key to KV Cache Pruning

**iclr2025** | 评分 3.5

### 问题背景

As context lenghts grows, the increasing size of Key and Value (KV) cache poses a significant challenge to efficiently serving Large Language Models (LLMs). KV cache pruning, by preserving only a small subset of important KV cache for sparse inference, is a recognized effective solution.

### 核心创新

As context lenghts grows, the increasing size of Key and Value (KV) cache poses a significant challenge to efficiently serving Large Language Models (LLMs)

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝, 稀疏化 | 对象: kv, weight, activation

---
##  Pretraining Context Compressor for Large Language Models with Embedding-Based Memory

**acl2025**

### 问题背景

Efficient processing of long contexts in large language models (LLMs) is essential for real-world applications like retrieval-augmented generation and in-context learning, especially in resource-constrained environments such as edge computing. This paper explores the embedding-based context compress

### 核心创新

Efficient processing of long contexts in large language models (LLMs) is essential for real-world applications like retrieval-augmented generation and in-context learning, especially in resource-const...

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ ProtMamba: a homology-aware but alignment-free protein state space model

**iclr2025** | 评分 5.8 | arXiv:2312.00752

### 问题背景

Protein design has important implications for drug discovery, personalized medicine, and biotechnology. Models based on multiple sequence alignments efficiently capture the evolutionary information in homologous protein sequences, but multiple sequence alignment construction is imperfect.

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: 通用优化 | 对象: kv

---
##  QCFuse: Query-Aware Cache Fusion via Compressed View for RAG Serving

**arXiv 2026** | arXiv:2606.05875

### 核心创新

QCFuse: Compressed-view query-aware selector; chunk-anchor query probing; critical-layer profiling

### 关键数据

1.7x prefill-time speedup over full prefill; 1.5x over ProphetKV

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★★ QSpec: Speculative Decoding with Complementary Quantization Schemes

**iclr2025** | 评分 5.8 | arXiv:2410.11305

### 问题背景

Quantization has been substantially adopted to accelerate inference and reduce memory consumption of large language models (LLMs). While activation-weight joint quantization speeds up the inference process through low-precision kernels, we demonstrate that it suffers severe performance degradation o

### 核心创新

QSpec: Low-precision joint quantization for drafting; high-precision for verification

### 关键数据

1.64x speedup without quality degradation

### 技术标签

方法: 量化 | 对象: kv, weight, activation

---
## ★ QSVD: Efficient Low-rank Approximation for Unified Query-Key-Value Weight Compression in Low-Precision Vision-Language M

**neurips2025** | 评分 4.8 | arXiv:2510.16292

### 问题背景

Vision-Language Models (VLMs) are integral to tasks such as image captioning and visual question answering, but their high computational cost, driven by large memory footprints and processing time, limits their scalability and real-time applicability. In this work, we propose leveraging Singular-Val

### 核心创新

QSVD: SVD over joint Q/K/V weight matrices; dynamic rank allocation + quantization

### 关键数据

>10% accuracy improvement vs quantization-only

### 技术标签

方法: 量化, 低秩 | 对象: kv, weight, activation

---
##  QuantSpec: Self-Speculative Decoding with Hierarchical Quantized KV Cache

**icml2025** | arXiv:2510.03929

### 问题背景

Large Language Models (LLMs) are increasingly being deployed on edge devices for long-context settings, creating a growing need for fast and efficient long-context inference. In these scenarios, the Key-Value (KV) cache is the primary bottleneck in terms of both GPU memory and latency, as the full K

### 核心创新

Self-speculative masked diffusions: Causal transformer for draft; parallel validation via speculative sampling

### 关键数据

~2X reduction in network forward passes (ICLR 2026)

### 技术标签

方法: 量化, 稀疏化 | 对象: kv, weight

---
##  R3Mem: Bridging Memory Retention and Retrieval via Reversible Compression

**acl2025** | arXiv:2502.15957

### 问题背景

Memory plays a key role in enhancing LLMsâ€™ performance when deployed to real-world applications. Existing solutions face trade-offs: explicit memory designs based on external storage require complex management and incur storage overhead, while implicit memory designs that store information via par

### 核心创新

R3Mem: Virtual memory tokens + hierarchical compression + reversible retrieval architecture

### 关键数据

State-of-art in long-context and RAG tasks

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  RandAR: Decoder-only Autoregressive Visual Generation in Random Orders

**cvpr2025** | arXiv:2507.04416

### 问题背景

We introduce RandAR, a decoder-only visual autoregressive (AR) model capable of generatng images in arbitrary token orders. Unlike previous decoder-only AR models that rely on a predefined generation order, RandAR removes this inductive bias, unlocking new capabilities in decoder-only generation.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  RAPID: Long-Context Inference with Retrieval-Augmented Speculative Decoding

**icml2025** | arXiv:2502.20330

### 问题背景

The emergence of long-context large language models (LLMs) offers a promising alternative to traditional retrieval-augmented generation (RAG) for processing extensive documents. However, the computational overhead of long-context inference presents significant efficiency challenges.

### 核心创新

RAPID: RAG drafter on shortened contexts; inference-time knowledge transfer; dual speedup + quality

### 关键数据

>2x speedup; improves quality (ICML 2025 Spotlight)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ RAT: Bridging RNN Efficiency and Attention Accuracy via Chunk-based Sequence Modeling

**neurips2025** | 评分 3.8 | arXiv:2507.04416

### 问题背景

Transformers have become the cornerstone of modern large-scale language models, but their reliance on softmax attention poses a computational bottleneck at both training and inference. Recurrent models offer high efficiency, but compressing the full sequence into a fixed-size and holistic representa

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ Reasoning Path Compression: Compressing Generation Trajectories for Efficient LLM Reasoning

**neurips2025** | 评分 4.5 | arXiv:2507.04416

### 问题背景

Recent reasoning-focused language models achieve high accuracy by generating lengthy intermediate reasoning paths before producing final answers. While this approach is effective in solving problems that require logical thinking, long reasoning paths significantly increase memory usage and reduce th

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★★ ReAttention: Training-Free Infinite Context with Finite Attention Scope

**iclr2025** | 评分 6.0

### 问题背景

The long-context capability of the Large Language Models (LLM) has made significant breakthroughs, but \textit{the maximum supported context length in length extrapolation} remains a critical bottleneck limiting their practical applications. The constraint of context length in LLMs arises from the s

### 核心创新

Continual Context Distillation with novel importance metrics; infinite context in fixed memory

### 关键数据

Outperforms long-context trained models on various NLP tasks

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Recurrent Context Compression: Efficiently Expanding the Context Window of LLM

**iclr2025** | 评分 4.0 | arXiv:2406.06110

### 问题背景

To extend the context length of Transformer-based large language models (LLMs) and improve comprehension capabilities, researchers often encounter constraints stemming from finite computational resources and bounded memory capacities. This work proposes a novel approach, termed Recurrent Context Com

### 核心创新

RCC: Recurrent context compression + instruction reconstruction; expands context window

### 关键数据

32x compression (BLEU4 ~0.95); 100% passkey at 1M

### 技术标签

方法: 通用优化 | 对象: kv

---
##  RedKnot: Head-Aware KV Reuse and SegPagedAttention

**arXiv 2026** | arXiv:2606.06256

### 核心创新

PagedAttention: Virtual memory and paging inspired attention; near-zero KV cache memory waste; flexible sharing within/across requests

### 关键数据

Efficient memory management for LLM serving (vLLM)

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  RefreshKV: Updating Small KV Cache During Long-form Generation

**acl2025** | arXiv:2411.05787

### 问题背景

Generating long sequences of tokens given a long-context input is a very compute-intensive inference scenario for large language models (LLMs). One prominent inference speed-up approach is constructing a smaller key-value (KV) cache, relieving LLMs from computing attention over a long sequence of to

### 核心创新

RefreshKV: Alternates between full context and subset attention; updates smaller KV cache based on attention patterns

### 关键数据

Efficient long-form generation

### 技术标签

方法: unclear | 对象: kv

---
## ★★ Regressing the Relative Future: Efficient Policy Optimization for Multi-turn RLHF

**iclr2025** | 评分 6.5

### 问题背景

Large Language Models (LLMs) have achieved remarkable success at tasks like summarization that involve a single turn of interaction. However, they can still struggle with multi-turn tasks like dialogue that require long-term planning.

### 核心创新

Large Language Models (LLMs) have achieved remarkable success at tasks like summarization that involve a single turn of interaction

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ RepoMaster: Autonomous Exploration and Understanding of GitHub Repositories for Complex Task Solving

**neurips2025** | 评分 4.2 | arXiv:2507.04416

### 问题背景

The ultimate goal of code agents is to solve complex tasks autonomously. Although large language models (LLMs) have made substantial progress in code generation, real-world tasks typically demand full-fledged code repositories rather than simple scripts.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 量化, 驱逐/剪枝, 低秩 | 对象: kv, weight

---
## ★ RetrievalAttention: Accelerating Long-Context LLM Inference via Vector Retrieval

**neurips2025** | 评分 4.5 | arXiv:2409.10516

### 问题背景

Transformer-based Large Language Models (LLMs) have become increasingly important. However, scaling LLMs to longer contexts incurs slow inference speed and high GPU memory consumption for caching key-value (KV) vectors.

### 核心创新

RetrievalAttention: ANNS indexes for KV vectors; attention-aware vector search; training-free

### 关键数据

1-3% data access; near full attention accuracy

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ RMAAT: A Bio-Inspired Approach for Efficient Long-Context Sequence Processing in Transformers

**iclr2025** | 评分 3.5 | arXiv:2601.00426

### 问题背景

Astrocytes, an essential component of the brain's neural circuitry, demonstrate learning capabilities through bioplausible mechanisms such as presynaptic plasticity and hebbian plasticity. However, their integration into computational models remains underexplored.

### 核心创新

RMAAT: Astrocyte-inspired memory compression; recurrent segment processing; retention factor

### 关键数据

Competitive accuracy on LRA benchmark; substantial efficiency gains

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ RobustKV: Defending Large Language Models against Jailbreak Attacks via KV Eviction

**iclr2025** | 评分 6.7

### 问题背景

Jailbreak attacks circumvent LLMs' built-in safeguards by concealing harmful queries within adversarial prompts. While most existing defenses attempt to mitigate the effects of adversarial prompts, they often prove inadequate as adversarial prompts can take arbitrary, adaptive forms.

### 核心创新

KV eviction defense by strategically removing lowest-ranked token KVs; jailbreak tokens lower importance of harmful query tokens

### 关键数据

Effectively counters SOTA jailbreak attacks while maintaining general performance on benign queries

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★ Rope to Nope and Back Again: A New Hybrid Attention Strategy

**neurips2025** | 评分 4.2 | arXiv:2507.04416

### 问题背景

Long-context large language models (LLMs) have achieved remarkable advancements, driven by techniques like Rotary Position Embedding (RoPE) (Su et al., 2023) and its extensions (Chen et al., 2023; Liu et al., 2024c; Peng et al., 2023). By adjusting RoPE parameters and incorporating training data wit

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  RoSTE: An Efficient Quantization-Aware Supervised Fine-Tuning Approach for Large Language Models

**icml2025** | arXiv:2502.09003

### 问题背景

Supervised fine-tuning is a standard method for adapting pre-trained large language models (LLMs) to downstream tasks. Quantization has been recently studied as a post-training technique for efficient LLM deployment.

### 核心创新

RoSTE: Rotated Straight-Through-Estimator; adaptive rotation for outlier reduction; QA-SFT

### 关键数据

Superior to post-SFT quantization (ICML 2025)

### 技术标签

方法: 量化 | 对象: kv, weight, activation

---
## ★ Routing Mamba: Scaling State Space Models with Mixture-of-Experts Projection

**neurips2025** | 评分 4.2 | arXiv:2312.00752

### 问题背景

State Space Models (SSMs) offer remarkable performance gains in efficient sequence modeling, with constant per-step inference-time computation and memory complexity. Recent advances, such as Mamba, further enhance SSMs with input-dependent gating and hardware-aware implementations, positioning them 

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: 稀疏化 | 对象: kv, weight, communication

---
## ★ Running Huge Context Windows On Tiny GPUs

**iclr2025** | 评分 4.7

### 问题背景

There is growing demand for large language models that can process hundreds of thousands or even millions of input tokens. Inference at this extreme scale demands significant computational resources and costs.

### 核心创新

There is growing demand for large language models that can process hundreds of thousands or even millions of input tokens

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  Sable: a Performant, Efficient and Scalable Sequence Model for MARL

**icml2025** | arXiv:2410.01706

### 问题背景

As multi-agent reinforcement learning (MARL) progresses towards solving larger and more complex problems, it becomes increasingly important that algorithms exhibit the key properties of (1) strong performance, (2) memory efficiency, and (3) scalability. In this work, we introduce Sable, a performant

### 核心创新

Sable: Retentive Networks adaptation for MARL; linear memory scaling with agent count

### 关键数据

Outperforms in 34/45 tasks; 1000+ agents with linear memory

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ SALS: Sparse Attention in Latent Space for KV Cache Compression

**neurips2025** | 评分 3.8 | arXiv:2510.24273

### 问题背景

Large Language Models (LLMs) capable of handling extended contexts are in high demand, yet their inference remains challenging due to substantial Key-Value (KV) cache size and high memory bandwidth requirements. Previous research has demonstrated that KV cache exhibits low-rank characteristics withi

### 核心创新

SALS: Low-rank projection to latent space; sparse token selection; RoPE-free

### 关键数据

6.4x KV compression; 5.7x attention speedup

### 技术标签

方法: 稀疏化, 低秩 | 对象: kv, weight

---
##  SAW-INT4: System-Aware 4-Bit KV-Cache Quantization for Real-World LLM Serving

**arXiv 2026** | arXiv:2604.19157

### 核心创新

SAW-INT4: Block-diagonal Hadamard rotation; system-aware design for paged memory; fused rotation-quantization kernel

### 关键数据

Near-lossless INT4; matches plain INT4 throughput

### 技术标签

方法: 量化 | 对象: kv

---
## ★ Scalable In-context Ranking with Generative Models

**neurips2025** | 评分 3.8 | arXiv:2507.04416

### 问题背景

In-context Ranking (ICR) is an emerging paradigm for Information Retrieval (IR), which leverages contextual understanding of LLMs by directly incorporating the task description, candidate documents, and the query into the model's input prompt and tasking the LLM to identify relevant document(s). Whi

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ Scale-invariant attention

**neurips2025** | 评分 4.0 | arXiv:2505.17083

### 问题背景

One persistent challenge in LLM research is the development of attention mechanisms that are able to generalise from training on shorter contexts to inference on longer contexts. We propose two conditions that we expect all effective long-context attention mechanisms to have: scale-invariant total a

### 核心创新

Scale-invariant total attention + sparsity; Gaussian assumption; position-dependent logit transformation

### 关键数据

Considerable validation loss improvement for zero-shot length generalization

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★★ Scaling Instruction-tuned LLMs to Million-token Contexts via Hierarchical Synthetic Data Generation

**iclr2025** | 评分 6.0 | arXiv:2507.04416

### 问题背景

Large Language Models (LLMs) struggle with long-context reasoning, not only due to the quadratic scaling of computational complexity with sequence length but also because of the scarcity and expense of annotating long-context data. There has been barely any open-source work that systematically ablat

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ Scaling up Test-Time Compute with Latent Reasoning: A Recurrent Depth Approach

**neurips2025** | 评分 5.0 | arXiv:2502.05171

### 问题背景

We study a novel language model architecture that is capable of scaling test-time computation by implicitly reasoning in latent space. Our model works by iterating a recurrent block, thereby unrolling to arbitrary depth at test-time.

### 核心创新

Recurrent depth: Unrolls at test-time in latent space; no specialized training data needed

### 关键数据

Scales to 3.5B params; up to 50B-equivalent compute

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  SCOPE: Optimizing Key-Value Cache Compression in Long-context Generation

**acl2025** | arXiv:2412.13649

### 问题背景

Key-Value (KV) cache has become a bottleneck of LLMs for long-context generation. Despite the numerous efforts in this area, the optimization for the decoding phase is generally ignored.

### 核心创新

SCOPE: Prefill/decoding phase optimization; sliding strategy for heavy hitters

### 关键数据

Efficient long-context generation

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Search-in-Context: Efficient Multi-Hop QA over Long Contexts via Monte Carlo Tree Search with Dynamic KV Retrieval

**acl2025**

### 问题背景

Recent advancements in large language models (LLMs) have demonstrated remarkable capabilities in complex reasoning tasks, such as math problem-solving and code generation. However, multi-hop question answering (MHQA) over long contexts, which demands both robust knowledge-intensive reasoning and eff

### 核心创新

Recent advancements in large language models (LLMs) have demonstrated remarkable capabilities in complex reasoning tasks, such as math problem-solving and code generation

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ SeCom: On Memory Construction and Retrieval for Personalized Conversational Agents

**iclr2025** | 评分 5.2 | arXiv:2502.05589

### 问题背景

To deliver coherent and personalized experiences in long-term conversations, existing approaches typically perform retrieval augmented response generation by constructing memory banks from conversation history at either the turn-level, session-level, or through summarization techniques. In this pape

### 核心创新

SeCom: Segment-level memory bank; conversation segmentation; compression-based denoising

### 关键数据

Significant advantage on LOCOMO and Long-MT-Bench+

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ SeerAttention: Learning Intrinsic Sparse Attention in Your LLMs

**iclr2025** | 评分 5.2 | arXiv:2410.13276

### 问题背景

Attention is the cornerstone of modern Large Language Models (LLMs). Yet its quadratic complexity limits the efficiency and scalability of LLMs, especially for those with a long-context window.

### 核心创新

SeerAttention: Learnable block-level sparsity via MoE gating

### 关键数据

Better accuracy and lower latency for long-context

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ SeerAttention: Self-distilled Attention Gating for Efficient Long-context Prefilling

**neurips2025** | 评分 4.2 | arXiv:2602.02180

### 问题背景

Attention is the cornerstone of modern Large Language Models (LLMs). Yet its quadratic complexity hinders efficiency and scalability, especially for long-context processing.

### 核心创新

STILL: Self-Saliency Score for token selection; Norm-Preserved Feature Map; chunk-wise parallelization

### 关键数据

86.2% improvement over prior linearized attention on long-context

### 技术标签

方法: 稀疏化, 蒸馏 | 对象: kv, weight, activation, communication

---
## ★★ Selecting Influential Samples for Long Context Alignment via Homologous Modelsâ€™ Guidance and Contextual Awareness Meas

**iclr2025** | 评分 5.0

### 问题背景

The expansion of large language models to effectively handle instructions with extremely long contexts has yet to be fully investigated. The primary obstacle lies in constructing a high-quality long instruction-following dataset devised for long context alignment.

### 核心创新

The expansion of large language models to effectively handle instructions with extremely long contexts has yet to be fully investigated

### 关键数据

See abstract for details

### 技术标签

方法: 量化 | 对象: kv

---
##  Self-Taught Agentic Long Context Understanding

**acl2025**

### 问题背景

Answering complex, long-context questions remains a major challenge for large language models (LLMs) as it requires effective question clarifications and context retrieval. We propose Agentic Long-Context Understanding (AgenticLU), a framework designed to enhance an LLMâ€™s understanding of such que

### 核心创新

Answering complex, long-context questions remains a major challenge for large language models (LLMs) as it requires effective question clarifications and context retrieval

### 关键数据

See abstract for details

### 技术标签

方法: 低秩 | 对象: kv

---
##  SepLLM: Accelerate Large Language Models by Compressing One Segment into One Separator

**icml2025** | arXiv:2412.12094

### 问题背景

Large Language Models (LLMs) have exhibited exceptional performance across a spectrum of natural language processing tasks. However, their substantial sizes pose considerable challenges, particularly in computational demands and inference speed, due to their quadratic complexity.

### 核心创新

SepLLM: Compresses segments between separators; separator token compression

### 关键数据

50% KV cache reduction; 4M token sequences

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Sharper and Faster mean Better: Towards More Efficient Vision-Language Model for Hour-scale Long Video Understanding

**acl2025**

### 问题背景

Despite existing multimodal language models showing impressive performance on the video understanding task, extremely long videos still pose significant challenges to language modelâ€™s context length, memory consumption, and computational complexity. To address these issues, we propose a vision-lan

### 核心创新

Despite existing multimodal language models showing impressive performance on the video understanding task, extremely long videos still pose significant challenges to language modelâ€™s context length...

### 关键数据

See abstract for details

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★★ SimLayerKV: A Simple Framework for Layer-Level KV Cache Reduction

**iclr2025** | 评分 5.2

### 问题背景

Recent advancements in large language models (LLMs) have extended their capabilities to handle long contexts. However, increasing the number of model layers and the length of input sequences significantly escalates the memory required to store key-value (KV) cache, posing challenges for efficient in

### 核心创新

Recent advancements in large language models (LLMs) have extended their capabilities to handle long contexts

### 关键数据

See abstract for details

### 技术标签

方法: 量化 | 对象: kv, weight

---
## ★ Sim-LLM: Optimizing LLM Inference at the Edge through Inter-Task KV Reuse

**neurips2025** | 评分 4.2

### 问题背景

KV cache technology, by storing key-value pairs, helps reduce the computational overhead incurred by *large language models* (LLMs). It facilitates their deployment on resource-constrained edge computing nodes like edge servers.

### 核心创新

KV cache technology, by storing key-value pairs, helps reduce the computational overhead incurred by *large language models* (LLMs)

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ SinkQ: Accurate 2-bit KV Cache Quantization with Dynamic Sink Tracking

**iclr2025** | 评分 4.4 | arXiv:2507.04416

### 问题背景

The impressive capabilities of large language models (LLMs) come at the cost of substantial computational resources during deployment. While KV Cache can significantly reduce recomputation during inference, it also introduces additional memory overhead.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 量化 | 对象: kv, communication

---
## ★ SmallKV: Small Model Assisted Compensation of KV Cache Compression for Efficient LLM Inference

**neurips2025** | 评分 4.2 | arXiv:2508.02751

### 问题背景

KV cache eviction has emerged as an effective solution to alleviate resource constraints faced by LLMs in long-context scenarios. However, existing token-level eviction methods often overlook two critical aspects: (1) their irreversible eviction strategy fails to adapt to dynamic attention patterns 

### 核心创新

SmallKV: Small model assisted compensation; attention matching between different-scale LLMs; addresses saliency shift and marginal information problems

### 关键数据

1.75-2.56x throughput improvement

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Smarter, Better, Faster, Longer: A Modern Bidirectional Encoder for Fast, Memory Efficient, and Long Context Finetuning 

**acl2025**

### 问题背景

Encoder-only transformer models such as BERT offer a great performance-size tradeoff for retrieval and classification tasks with respect to larger decoder-only models. Despite being the workhorse of numerous production pipelines, there have been limited Pareto improvements to BERT since its release.

### 核心创新

Encoder-only transformer models such as BERT offer a great performance-size tradeoff for retrieval and classification tasks with respect to larger decoder-only models

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ SOLOS: Sparse Optimization For Long Sequence In Context Compression Enhanced LLMs

**iclr2025** | 评分 4.8

### 问题背景

Recent advances in long-context large language models (LLMs) make them commercially viable, but their standard attention mechanisms' quadratic complexity hinders deployment due to excessive computational costs. To address this, researchers have explored Q-former-like architectures that compress inpu

### 核心创新

Recent advances in long-context large language models (LLMs) make them commercially viable, but their standard attention mechanisms' quadratic complexity hinders deployment due to excessive computatio...

### 关键数据

See abstract for details

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  Sparse Video-Gen: Accelerating Video Diffusion Transformers with Spatial-Temporal Sparsity

**icml2025** | arXiv:2507.04416

### 问题背景

Diffusion Transformers (DiTs) dominate video generation but their high computational cost severely limits real-world applicability, usually requiring tens of minutes to generate a few seconds of video even on high-performance GPUs. This inefficiency primarily arises from the quadratic computational 

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  SpectrumKV: Per-Token Mixed-Precision KV Cache Transfer for Prefill-Decode Disaggregated LLM Serving

**arXiv 2026** | arXiv:2606.08635

### 核心创新

SpectrumKV: Per-token mixed-precision (FP16/INT8/INT4); attention sink protection; lightweight deployment-time probe for INT4 tolerance

### 关键数据

50-62% TTFT reduction at 50% budget; near-lossless perplexity

### 技术标签

方法: 量化 | 对象: kv, communication

---
## ★ Speculate Deep and Accurate: Lossless and Training-Free Acceleration for Offloaded LLMs via Substitute Speculative Decod

**neurips2025** | 评分 4.0 | arXiv:2507.04416

### 问题背景

The immense model sizes of large language models (LLMs) challenge deployment on memory-limited consumer GPUs. Although model compression and parameter offloading are common strategies to address memory limitations, compression can degrade quality, and offloading maintains quality but suffers from sl

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 量化 | 对象: kv, weight

---
## ★★ Speculative RAG: Enhancing Retrieval Augmented Generation through Drafting

**iclr2025** | 评分 5.5 | arXiv:2507.04416

### 问题背景

Retrieval augmented generation (RAG) combines the generative abilities of large language models (LLMs) with external knowledge sources to provide more accurate and up-to-date responses. Recent RAG advancements focus on improving retrieval outcomes through iterative LLM refinement or self-critique ca

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv

---
##  SPEX: Scaling Feature Interaction Explanations for LLMs

**icml2025** | arXiv:2502.13870

### 问题背景

Large language models (LLMs) have revolutionized machine learning due to their ability to capture complex interactions between input features. Popular post-hoc explanation methods like SHAP provide *marginal* feature attributions, while their extensions to interaction importances only scale to small

### 核心创新

SPEX: Spectral Explainer; sparse Fourier transform; channel decoding; scales to ~1000 tokens

### 关键数据

20% improvement over marginal attribution

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ Spotlight Attention: Towards Efficient LLM Generation via Non-linear Hashing-based KV Cache Retrieval

**neurips2025** | 评分 4.6 | arXiv:2508.19740

### 问题背景

Reducing the key-value (KV) cache burden in Large Language Models (LLMs) significantly accelerates inference. Dynamically selecting critical KV caches during decoding helps maintain performance.

### 核心创新

Spotlight Attention: Non-linear hashing for KV cache retrieval; optimizes embedding distribution; Bradley-Terry ranking loss

### 关键数据

5x shorter hash codes; 3x end-to-end throughput; 512K tokens in 100μs on A100

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  Squeezed Attention: Accelerating Long Context Length LLM Inference

**acl2025** | arXiv:2411.09688

### 问题背景

Emerging Large Language Model (LLM) applications require long input context in order to perform complex tasks like document analysis and code generation.For these long context length applications, the length of the input prompt poses a significant challenge in terms of inference efficiency since the

### 核心创新

Squeezed Attention: Offline K-means clustering of fixed context keys; centroid-based semantic relevance prediction

### 关键数据

Accelerates long context LLM inference with reduced bandwidth and compute

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  Streamlining the Collaborative Chain of Models into A Single Forward Pass in Generation-Based Tasks

**acl2025** | arXiv:2507.04416

### 问题背景

In Retrieval-Augmented Generation (RAG) and agent-based frameworks, the â€œChain of Modelsâ€ approach is widely used, where multiple specialized models work sequentially on distinct sub-tasks. This approach is effective but increases resource demands as each model must be deployed separately.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ Stuffed Mamba: State Collapse and State Capacity of RNN-Based Long-Context Modeling

**iclr2025** | 评分 3.6 | arXiv:2312.00752

### 问题背景

One essential advantage of recurrent neural networks (RNNs) over transformer-based language models is their linear computational complexity concerning the sequence length, which makes them much faster in handling long sequences during inference. However, most publicly available RNNs (e.g., Mamba and

### 核心创新

Mamba: Selective state spaces; linear-time sequence modeling; addresses Transformer computational inefficiency

### 关键数据

Subquadratic-time; competes with attention on language

### 技术标签

方法: unclear | 对象: kv, weight

---
##  Sub-Token Routing for KV Cache Compression

**arXiv 2026** | arXiv:2604.21335

### 核心创新

Sub-Token Routing: Splits retained value vectors into groups; finer control axis inside retained tokens; works after token-level reduction

### 关键数据

Improves token-level reduction in LLM and VLM settings; larger gains at smaller KV budgets

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ Tail-Optimized Caching for LLM Inference

**neurips2025** | 评分 3.8 | arXiv:2510.15152

### 问题背景

Prompt caching is critical for reducing latency and cost in LLM inference---OpenAI and Anthropic report up to 50â€“90\% cost savings through prompt reuse. Despite its widespread success, little is known about what constitutes an optimal prompt caching policy, particularly when optimizing tail latenc

### 核心创新

Tail-Optimized LRU: Two-line modification; reallocates KV cache capacity to prioritize high-latency conversations

### 关键数据

Optimizes tail latency vs LRU which can perform arbitrarily poorly

### 技术标签

方法: 通用优化 | 对象: kv

---
##  TailorKV: A Hybrid Framework for Long-Context Inference via Tailored KV Cache Optimization

**acl2025** | arXiv:2505.19586

### 问题背景

The Key-Value (KV) cache in generative large language models (LLMs) introduces substantial memory overhead. Existing works mitigate this burden by offloading or compressing the KV cache.

### 核心创新

TailorKV: Hybrid loading + quantization; dominant tokens loaded, others quantized; layer-specific treatment

### 关键数据

Complements loading and quantization per layer

### 技术标签

方法: 量化 | 对象: kv, activation

---
## ★ Taipan: Efficient and Expressive State Space Language Models with Selective Attention

**iclr2025** | 评分 4.2 | arXiv:2410.18572

### 问题背景

Efficient long-context language modeling remains a significant challenge in Natural Language Processing (NLP). While Transformers dominate language tasks, they struggle with long sequences due to quadratic computational complexity in training and linearly scaling memory costs during inference.

### 核心创新

Taipan: Hybrid Mamba-2 + Selective Attention Layers (SALs); identifies long-range interaction tokens

### 关键数据

Efficient long-context language modeling

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Tangram: Non-Uniform KV Cache Compression for Multi-turn LLM Serving

**arXiv 2026** | arXiv:2606.06302

### 核心创新

Tangram: Budget Reservation; Ragged Paging for heterogeneous heads; Ahead-of-Time Load Balancing; static resolution of head-wise retention

### 关键数据

2.6x throughput improvement over full-KV baseline

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ Technical Debt in In-Context Learning: Diminishing Efficiency in Long Context

**neurips2025** | 评分 4.2 | arXiv:2502.04580

### 问题背景

Transformers have demonstrated remarkable in-context learning (ICL) capabilities, adapting to new tasks by simply conditioning on demonstrations without parameter updates. Compelling empirical and theoretical evidence suggests that ICL, as a general-purpose learner, could outperform task-specific mo

### 核心创新

ICL efficiency analysis via meta-ICL framework; Bayes optimal comparison; information-theoretic analysis

### 关键数据

Reveals diminishing efficiency in long context ICL

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  The Rotary Position Embedding May Cause Dimension Inefficiency in Attention Heads for Long-Distance Retrieval

**acl2025**

### 问题背景

The Rotary Position Embedding (RoPE) is widely used in the attention heads of many large language models (LLM). It rotates dimensions in the query and the key vectors by different angles according to their positions in the input sequence.

### 核心创新

The Rotary Position Embedding (RoPE) is widely used in the attention heads of many large language models (LLM)

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★★ TidalDecode: Fast and Accurate LLM Decoding with Position Persistent Sparse Attention

**iclr2025** | 评分 5.8 | arXiv:2410.05076

### 问题背景

Large language models (LLMs) have driven significant advancements across diverse NLP tasks, with long-context models gaining prominence for handling extended inputs. However, the expanding key-value (KV) cache size required by Transformer architectures intensifies the memory constraints, particularl

### 核心创新

TidalDecode: Position persistent sparse attention; spatial coherence across layers; full attention token selection layers

### 关键数据

Up to 2.1x latency reduction; matches full attention quality

### 技术标签

方法: 稀疏化 | 对象: kv

---
##  TokenDance: Scaling Multi-Agent LLM Serving via Collective KV Cache Sharing

**arXiv 2026** | arXiv:2604.03143

### 核心创新

TokenDance: Collective KV Cache sharing via All-Gather pattern; KV Collector; Diff-Aware Storage with block-sparse diffs

### 关键数据

2.7x more concurrent agents; 17.5x KV storage reduction; 1.9x prefill speedup

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, communication

---
##  TopV: Compatible Token Pruning with Inference Time Optimization for Fast and Low-Memory Multimodal Vision Language Model

**cvpr2025** | arXiv:2503.18278

### 问题背景

Vision-Language Models (VLMs) demand substantial computational resources during inference, largely due to the extensive visual input tokens for representing visual information. Previous studies have noted that visual tokens tend to receive less attention than text tokens, suggesting their lower impo

### 核心创新

TopV: Compatible token pruning with FlashAttention/KV cache; inference time optimization

### 关键数据

Reduces KV cache size during prefilling without training

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, weight

---
## ★★ Training Free Exponential Context Extension via Cascading KV Cache

**iclr2025** | 评分 6.0

### 问题背景

The transformer's context window is vital for tasks such as few-shot learning and conditional generation as it preserves previous tokens for active memory. However, as the context lengths increase, the computational costs grow quadratically, hindering the deployment of large language models (LLMs) i

### 核心创新

Scalable multi-cartridge learning with dynamic distractor mixing; memory-efficient budget manager; hundreds of per-document cartridges

### 关键数据

10-31 points improvement over monolithic cartridge; 3-4x fewer prompt tokens vs RAG

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Transcending Cost-Quality Tradeoff in Agent Serving via Session-Awareness

**neurips2025** | 评分 4.0

### 问题背景

Large Language Model (LLM) agents are capable of task execution across various domains by autonomously interacting with environments and refining LLM responses based on feedback. However, existing model serving systems are not optimized for the unique demands of serving agents.

### 核心创新

Large Language Model (LLM) agents are capable of task execution across various domains by autonomously interacting with environments and refining LLM responses based on feedback

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ TransMLA: Migrating GQA Models to MLA with Full DeepSeek Compatibility and Speedup

**neurips2025** | 评分 4.2 | arXiv:2502.07864

### 问题背景

Modern large-language models often face communication bottlenecks on current hardware rather than computational limitations. *Multi-head latent attention (MLA)* addresses this by compressing the key-value cache using low-rank matrices, while the Absorb operation prevents the KV cache from reverting 

### 核心创新

TransMLA: Converts GQA models to MLA; DeepSeek-compatible; 93% KV cache compression

### 关键数据

10.6x inference speedup at 8K context for LLaMA-2-7B

### 技术标签

方法: 量化, 低秩 | 对象: kv, weight

---
##  TriAxialKV: Toward Extreme Low-Precision KV-Cache Quantization for Agentic Inference Tasks

**arXiv 2026** | arXiv:2605.17170

### 核心创新

TriAxialKV: INT2/INT4 mixed precision based on triaxial tags (temporal recency, modality, semantic role); per-tag sensitivity calibration

### 关键数据

4.5x KV cache size with matched accuracy on OSWorld computer-use agent

### 技术标签

方法: 量化 | 对象: kv

---
## ★ Twilight: Adaptive Attention Sparsity with Hierarchical Top-$p$ Pruning

**neurips2025** | 评分 4.6 | arXiv:2502.02770

### 问题背景

Leveraging attention sparsity to accelerate long-context large language models (LLMs) has been of great importance recently. However, most existing sparse attention algorithms use a fixed budget of how many tokens to use in their computations.

### 核心创新

Twilight: Hierarchical top-p pruning (nucleus sampling for attention); adaptive budgeting

### 关键数据

Up to 98% redundant token pruning

### 技术标签

方法: 驱逐/剪枝, 稀疏化 | 对象: kv, weight, activation

---
## ★★ UNComp: Uncertainty-Aware Long-context Compressor for Efficient Large Language Model Inference

**iclr2025** | 评分 5.5 | arXiv:2410.03090

### 问题背景

Deploying large language models (LLMs) is challenging due to their high memory and computational demands, especially during long-context inference. While key-value (KV) caching accelerates inference by reusing previously computed keys and values, it also introduces significant memory overhead.

### 核心创新

UNComp: Truncated matrix entropy for uncertainty-aware sparsity detection; adaptive compression; identifies retrieval heads and layers

### 关键数据

4.74% KV cache size; 6% prefill speedup; 6.4x throughput improvement

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ UniGist: Towards General and Hardware-aligned Sequence-level Long Context Compression

**neurips2025** | 评分 4.0 | arXiv:2509.15763

### 问题背景

Large language models are increasingly capable of handling long-context inputs, but the memory overhead of KV cache remains a major bottleneck for general-purpose deployment. While many compression strategies have been explored, sequence-level compression is particularly challenging due to its tende

### 核心创新

UniGist: Sequence-level compression with gist tokens; chunk-free training; hardware-aligned

### 关键数据

Strong detail-recalling tasks and long-range dependency

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  UniICL: An Efficient ICL Framework Unifying Compression, Selection, and Generation

**acl2025** | arXiv:2405.17062

### 问题背景

In-context learning (ICL) enhances the reasoning abilities of Large Language Models (LLMs) by prepending a few demonstrations. It motivates researchers to introduce more examples to provide additional contextual information for the generation.

### 核心创新

UniICL: Unifies compression, selection, generation; Demonstration Bank caching

### 关键数据

Avoids repeated compression of same demonstrations

### 技术标签

方法: 通用优化 | 对象: kv

---
##  Unlimited OCR: Constant KV Cache via Reference Sliding Window Attention

**arXiv 2026** | arXiv:2606.23050

### 核心创新

Reference Sliding Window Attention (R-SWA): Constant KV cache for OCR; dozens of pages in single forward pass

### 关键数据

Constant KV cache throughout entire decoding process for long document transcription

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  Value Residual Learning

**acl2025**

### 问题背景

While Transformer models have achieved remarkable success in various domains, the effectiveness of information propagation through deep networks remains a critical challenge. Standard hidden state residuals often fail to adequately preserve initial token-level information in deeper layers.

### 核心创新

While Transformer models have achieved remarkable success in various domains, the effectiveness of information propagation through deep networks remains a critical challenge

### 关键数据

See abstract for details

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
##  VASparse: Towards Efficient Visual Hallucination Mitigation via Visual-Aware Token Sparsification

**cvpr2025** | arXiv:2501.06553

### 问题背景

Large Vision-Language Models (LVLMs) may produce outputs that are unfaithful to reality, also known as visual hallucinations (VH), which significantly impedes their real-world usage. To alleviate VH, various decoding strategies have been proposed to enhance visual information.

### 核心创新

VASparse: Visual-aware token sparsification; mitigates visual hallucination; sparse visual contrastive decoding

### 关键数据

State-of-art VH mitigation; competitive decoding speed

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
## ★ Vgent: Graph-based Retrieval-Reasoning-Augmented Generation For Long Video Understanding

**neurips2025** | 评分 4.8 | arXiv:2507.04416

### 问题背景

Understanding and reasoning over long videos pose significant challenges for large video language models (LVLMs) due to the difficulty in processing intensive video tokens beyond context window and retaining long-term sequential information. Retrieval-Augmented Generation (RAG) has demonstrated effe

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
##  ViCoStream: Streaming VideoLLMs Can Run Beyond 100 FPS with Stage-Wise Coordinated Inference

**arXiv 2026** | arXiv:2606.19849

### 核心创新

ViCoStream: Stage-wise coordinated streaming; chunk-wise execution; CUDA-stream overlap; bounded visual attention

### 关键数据

134 FPS video throughput; <50ms TTFT on A100

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---
## ★ VideoMAR: Autoregressive Video Generation with Continuous Tokens

**neurips2025** | 评分 4.0 | arXiv:2507.04416

### 问题背景

Masked-based autoregressive models have demonstrated promising image generation capability in continuous space. However, their potential for video generation remains under-explored.

### 核心创新

RAT: Chunk-based recurrence + cross-chunk softmax attention; hybrid architecture

### 关键数据

7x training speedup at 100K; 9x generation at 4K (NeurIPS 2025)

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★★ VideoWebArena: Evaluating Long Context Multimodal Agents with Video Understanding Web Tasks

**iclr2025** | 评分 6.2 | arXiv:2410.19100

### 问题背景

Videos are often used to learn or extract the necessary information to complete
tasks in ways different than what text or static imagery can provide. However, many
existing agent benchmarks neglect long-context video understanding, instead focus-
ing on text or static image inputs.

### 核心创新

VideoWA: 2021 web agent tasks with video tutorials; skill vs factual retention taxonomy

### 关键数据

4 hours video content; benchmark for long-context video agents

### 技术标签

方法: 通用优化 | 对象: kv

---
## ★ Vision Function Layer in Multimodal LLMs

**neurips2025** | 评分 4.2 | arXiv:2509.24791

### 问题背景

This study identifies that visual-related functional decoding is distributed across different decoder layers in Multimodal Large Language Models (MLLMs). Typically, each function, such as counting, grounding, or OCR recognition, narrows down to two or three layers, which we define as Vision Function

### 核心创新

VFL: Visual Token Swapping analysis; layer-specific function identification; VFL-LoRA + VFL-select

### 关键数据

98% of full-data performance with 20% data

### 技术标签

方法: 低秩 | 对象: kv, weight, communication

---
## ★★ VL-Cache: Sparsity and Modality-Aware KV Cache Compression for Vision-Language Model Inference Acceleration

**iclr2025** | 评分 6.0 | arXiv:2410.23317

### 问题背景

Vision-Language Models (VLMs) have demonstrated impressive performance across a versatile set of tasks. A key challenge in accelerating VLMs is storing and accessing the large Key-Value (KV) cache that encodes long visual contexts, such as images or videos.

### 核心创新

VL-Cache: Sparsity and modality-aware compression for VLM; layer-adaptive budget allocation; modality-aware token scoring

### 关键数据

90% KV memory reduction; 7.08x decoding speedup; 10% KV cache retention

### 技术标签

方法: 稀疏化 | 对象: kv, activation

---
##  VoCo-LLaMA: Towards Vision Compression with Large Language Models

**cvpr2025** | arXiv:2406.12275

### 问题背景

Vision-Language Models (VLMs) have achieved remarkable success in various multi-modal tasks, but they are often bottlenecked by the limited context window and high computational cost of processing high-resolution image inputs and videos. Vision compression can alleviate this problem by reducing the 

### 核心创新

VoCo-LLaMA: Vision compression via LLMs; Vision Compression tokens; attention distillation; temporal correlation

### 关键数据

576x compression ratio; 94.8% fewer FLOPs; 69.6% inference acceleration

### 技术标签

方法: 蒸馏 | 对象: kv

---
##  When Less Latent Leads to Better Relay: Information-Preserving Compression for Latent Multi-Agent LLM Collaboration

**arXiv 2026** | arXiv:2604.13349

### 核心创新

Orthogonal Backfill (OBF): Low-rank orthogonal residual from discarded KV; mitigates hard eviction information loss

### 关键数据

79.8-89.4% communication cost reduction; comparable to full KV relay on 9 benchmarks

### 技术标签

方法: 驱逐/剪枝 | 对象: kv, communication

---
## ★★ World Model on Million-Length Video And Language With Blockwise RingAttention

**iclr2025** | 评分 5.8 | arXiv:2502.16249

### 问题背景

Enabling long-context understanding remains a key challenge in scaling existing sequence models -- a crucial component in developing generally intelligent models that can process and operate over long temporal horizons that potentially consist of millions of tokens. In this paper, we aim to address 

### 核心创新

LION: Bidirectional Linear Transformer framework; full/RNN/chunkwise parallel forms

### 关键数据

Matches softmax Transformers; faster training (NeurIPS 2025)

### 技术标签

方法: 低秩 | 对象: kv, weight

---
## ★★ Writing in the Margins: Better Inference Patterns for Long-Context Retrieval

**iclr2025** | 评分 6.0 | arXiv:2408.14906

### 问题背景

In this paper, we introduce Writing in the Margins (WiM), a new inference pattern for Large Language Models designed to optimize the handling of long input sequences in retrieval-oriented tasks. This approach leverages the chunked prefill of the key-value cache to perform segment-wise inference, whi

### 核心创新

WiM: Chunked prefill with segment-wise inference; generates intermediate 'margins' for guidance

### 关键数据

Enhances retrieval performance without fine-tuning

### 技术标签

方法: 通用优化 | 对象: kv

---
##  XAttention: Block Sparse Attention with Antidiagonal Scoring

**icml2025** | arXiv:2503.16428

### 问题背景

Long-Context Transformer Models (LCTMs) are vital for real-world applications but suffer high computational costs due to attention's quadratic complexity. Block-sparse attention mitigates this by focusing computation on critical regions, yet existing methods struggle with balancing accuracy and effi

### 核心创新

XAttention: Antidiagonal scoring for block importance; block sparse attention; plug-and-play

### 关键数据

13.5x attention speedup; comparable to full attention

### 技术标签

方法: 驱逐/剪枝, 稀疏化 | 对象: kv, weight, activation

---
##  YOCO++: Enhancing YOCO with KV Residual Connections for Efficient LLM Inference

**arXiv 2026** | arXiv:2604.13556

### 核心创新

YOCO++: Weighted residual connection between bottom-half layers and bottom layer for cross-layer KV compression

### 关键数据

State-of-the-art at 50% compression rate among cross-layer methods

### 技术标签

方法: 低秩 | 对象: kv

---
##  YouZhi: Towards High-Concurrency Financial LLMs via Adaptive GQA-to-MLA Transition

**arXiv 2026** | arXiv:2606.05868

### 核心创新

YouZhi: Layer-adaptive GQA-to-MLA transition with per-layer FreqFold sizes; generalized knowledge distillation + financial SFT

### 关键数据

2.69x concurrency (7B), 2.43x (14B); 12.3% financial benchmark improvement

### 技术标签

方法: 蒸馏 | 对象: kv

---
## ★ ZeroS: Zeroâ€‘Sum Linear Attention for Efficient Transformers

**neurips2025** | 评分 4.2 | arXiv:2602.05230

### 问题背景

Linear attention methods offer Transformers $O(N)$ complexity but typically underperform standard softmax attention. We identify two fundamental limitations affecting these approaches: the restriction to convex combinations that only permits additive information blending, and uniform accumulated wei

### 核心创新

ZeroS: Removes zero-order term 1/t; zero-sum softmax residuals; contrastive operations

### 关键数据

Matches softmax attention at O(N) complexity

### 技术标签

方法: 通用优化 | 对象: kv, weight

---
## ★ ZipVL: Efficient Large Vision-Language Models with Dynamic Token Sparsification and KV Cache Compression

**iclr2025** | 评分 4.0 | arXiv:2410.08584

### 问题背景

The efficiency of large vision-language models (LVLMs) is constrained by the computational bottleneck of the attention mechanism during the prefill phase and the memory bottleneck of fetching the key-value (KV) cache in the decoding phase, particularly in scenarios involving high-resolution images o

### 核心创新

ZipVL: Dynamic token sparsification; layer-specific attention ratio allocation; important token selection

### 关键数据

2.3x prefill speedup; 2.8x decoding throughput; 0.5% accuracy loss

### 技术标签

方法: 量化, 稀疏化 | 对象: kv, weight, activation

---
##  ZoomR: Memory Efficient Reasoning through Multi-Granularity Key Value Retrieval

**arXiv 2026** | arXiv:2604.10898

### 核心创新

ZoomR: Multi-granularity KV retrieval; summary keys as coarse index; hierarchical strategy with zoom-in to fine details

### 关键数据

4x memory reduction with competitive reasoning performance

### 技术标签

方法: 驱逐/剪枝 | 对象: kv

---

*精读日期：2026-06-28*
